本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[190. 颠倒二进制位 - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-bits/description/?envType=daily-question&envId=2026-02-16)

>标签：位运算  |  分治

# 一、题目描述

### **问题概括**

给定一个 32 位有符号整数 `n`（范围为 `0` 到 `2^31-2` 且为偶数），请将其二进制位（共 32 位）进行颠倒，并返回颠倒后得到的整数。

### **示例说明**

- 输入 `n = 43261596`，其二进制为 `00000010100101000001111010011100`，颠倒后得到 `00111001011110000010100101000000`，对应整数 `964176192`。
- 输入 `n = 2147483644`，二进制为 `01111111111111111111111111111100`，颠倒后得到 `00111111111111111111111111111110`，对应整数 `1073741822`。

### **进阶要求**

如果该函数需要被多次调用，可以考虑优化算法（例如使用预计算或查表）。



# 二、解题思路\解题过程

## ✅ 带详细注释的代码

```java
class Solution {
    // 掩码定义（32位）
    private static final int m0 = 0x55555555; // 二进制: 01010101 01010101 ... （奇数位为1）
    private static final int m1 = 0x33333333; // 二进制: 00110011 00110011 ... （每2位一组，低2位为1）
    private static final int m2 = 0x0f0f0f0f; // 二进制: 00001111 00001111 ... （每4位一组，低4位为1）
    private static final int m3 = 0x00ff00ff; // 二进制: 0000000011111111 0000000011111111 （每8位一组）

    public int reverseBits(int n) {
        // 第1步：交换所有相邻的1位（奇偶位互换）
        // 例如：abcdefgh → badcfehg
        n = (n >>> 1 & m0) | ((n & m0) << 1);

        // 第2步：以2位为单位，交换相邻的2位组
        // 例如：badcfehg → dcba hgfe
        n = (n >>> 2 & m1) | ((n & m1) << 2);

        // 第3步：以4位为单位，交换相邻的4位组
        // 例如：dcba hgfe → hgfedcba（对每8位内部完成反转）
        n = (n >>> 4 & m2) | ((n & m2) << 4);

        // 第4步：以8位为单位，交换相邻的8位组（即字节交换）
        n = (n >>> 8 & m3) | ((n & m3) << 8);

        // 第5步：交换高16位和低16位
        // 完成整个32位的反转
        return (n >>> 16) | (n << 16);
    }
}
```

> 💡 注意：使用 `>>>`（无符号右移），避免符号位扩展影响结果。

---

## 🧠 核心解题思想分析

### 🔍 问题目标：

给定一个 32 位无符号整数，**将其二进制位顺序完全颠倒**，并返回结果。

> 例如：  
> 输入：`00000010100101000001111010011100`  
> 输出：`00111001011110000010100101000000`

---

### 💡 核心思想：**分治法（Divide and Conquer） + 位并行操作**

不是一位一位地移动，而是**逐层合并**：  
**1位 ↔ 2位 ↔ 4位 ↔ 8位 ↔ 16位**，最终完成 32 位反转。

这类似于 **归并排序的逆过程**，但用位运算并行处理所有位。

---

### 🔬 每一步详解（以 8 位简化版演示）

假设我们有 8 位：`a b c d e f g h`，目标是变成 `h g f e d c b a`

#### ✅ 步骤1：交换相邻 1 位

- 使用掩码 `m0 = 01010101`
- `(n & m0)` 提取奇数位（b, d, f, h）
- `(n >>> 1 & m0)` 提取偶数位（a, c, e, g）并右移
- 合并后：`b a d c f e h g`

#### ✅ 步骤2：交换相邻 2 位组

- 掩码 `m1 = 00110011`
- 提取低2位组（ba, dc, fe, hg）
- 交换相邻组 → `d c b a h g f e`

#### ✅ 步骤3：交换相邻 4 位组

- 掩码 `m2 = 00001111`
- 交换高低4位 → `h g f e d c b a`（8位完成）

#### ✅ 扩展到 32 位：

- 再加两步：8位↔8位（字节交换），16位↔16位（高低半字交换）

---

### 📊 掩码作用表

| 步骤 | 单元大小 | 掩码（十六进制） | 掩码（二进制片段）    | 操作         |
| ---- | -------- | ---------------- | --------------------- | ------------ |
| 1    | 1 位     | `0x55555555`     | `01010101...`         | 奇偶位互换   |
| 2    | 2 位     | `0x33333333`     | `00110011...`         | 2位组互换    |
| 3    | 4 位     | `0x0f0f0f0f`     | `00001111...`         | 4位组互换    |
| 4    | 8 位     | `0x00ff00ff`     | `0000000011111111...` | 字节互换     |
| 5    | 16 位    | —                | —                     | 高低16位互换 |

> ✅ 每一步都**并行处理整个32位**，效率极高！

---

### ⏱️ 复杂度分析：

- **时间复杂度**：O(1)  
    —— 固定 5 步位运算，与输入无关
- **空间复杂度**：O(1)  
    —— 只用常数个变量

> 💡 这是理论最优解！比循环32次快得多（尤其在硬件支持位运算时）

---

## 📚 笔记总结

### 🧩 核心思想：

> **分治法 + 位并行操作（Bit-level Parallelism）**

### ✅ 算法步骤（32位）：

1. **1位交换**：奇偶位互换 → `m0 = 0x55555555`
2. **2位交换**：相邻2位组互换 → `m1 = 0x33333333`
3. **4位交换**：相邻4位组互换 → `m2 = 0x0f0f0f0f`
4. **8位交换**：相邻字节互换 → `m3 = 0x00ff00ff`
5. **16位交换**：高低16位互换 → 无掩码，直接移位

### 🔑 关键技巧：

- 使用**无符号右移 `>>>`** 避免符号扩展；
- **掩码提取特定位置**，移位后合并；
- **每一步都是可逆且对称的**，适用于任意 2^k 位反转。

### ⏱️ 复杂度：

- 时间：O(1)
- 空间：O(1)

### 💡 应用场景：

- 哈希函数、加密算法中的位反转；
- FFT（快速傅里叶变换）中的位序重排；
- 高性能计算中的数据预处理。

---

# 三、代码演示

## Java代码

```Java
class Solution {
    // 掩码定义（32位）
    private static final int m0 = 0x55555555;  // 二进制：01010101 01010101 ... （奇数位为1）
    private static final int m1 = 0x33333333;  // 二进制：00110011 00110011 ... （每2位一组，低2位为1）
    private static final int m2 = 0x0f0f0f0f;  // 二进制：00001111 00001111 ... （每4位一组，低4位为1）
    private static final int m3 = 0x00ff00ff;  // 二进制：0000000011111111 0000000011111111 （每8位一组）
    public int reverseBits(int n) {
        // 第1步：交换所有相邻的1位（奇偶位互换）
        n = (n >>> 1 & m0) | ((n & m0) << 1);

        // 第2步：以2位为单位，交换相邻的2位组
        n = (n >>> 2 & m1) | ((n & m1) << 2);

        // 第3步：以4位为单位，交换相邻的4位组
        n = (n >>> 4 & m2) | ((n & m2) << 4);

        // 第4步：以8位为单位，交换相邻的8位组（即字节交换）
        n = (n >>> 8 & m3) | ((n & m3) << 8);

        // 第5步：交换高16位和低16位
        return (n >>> 16) | (n << 16);
    }
}
```

![190_颠倒二进制位_Java](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/5_%E4%BD%8D%E8%BF%90%E7%AE%97/Images/190_%E9%A2%A0%E5%80%92%E4%BA%8C%E8%BF%9B%E5%88%B6%E4%BD%8D_Java.png)

## Python代码

```Python
class Solution:
    # 掩码定义（32位）
    m0 = 0x55555555  # 二进制：01010101 01010101 ... （奇数位为1）
    m1 = 0x33333333  # 二进制：00110011 00110011 ... （每2位一组，低2位为1）
    m2 = 0x0f0f0f0f  # 二进制：00001111 00001111 ... （每4位一组，低4位为1）
    m3 = 0x00ff00ff  # 二进制：0000000011111111 0000000011111111 （每8位一组）
    
    def reverseBits(self, n: int) -> int:
        # 第1步：交换所有相邻的1位（奇偶位互换）
        n = (n >> 1 & self.m0) | ((n & self.m0) << 1)

        # 第2步：以2位为单位，交换相邻的2位组
        n = (n >> 2 & self.m1) | ((n & self.m1) << 2)

        # 第3步：以4位为单位，交换相邻的4位组
        n = (n >> 4 & self.m2) | ((n & self.m2) << 4)

        # 第4步：以8位为单位，交换相邻的8位组（即字节交换）
        n = (n >> 8 & self.m3) | ((n & self.m3) << 8)

        # 第5步：交换高16位和低16位
        return (n >> 16 | (n << 16)) & 0xFFFFFFFF
```



## C++代码

```C++
class Solution {
public:
    // 掩码定义（32位）
    static const int m0 = 0x55555555;  // 二进制：01010101 01010101 ... （奇数位为1）
    static const int m1 = 0x33333333;  // 二进制：00110011 00110011 ... （每2位一组，低2位为1）
    static const int m2 = 0x0f0f0f0f;  // 二进制：00001111 00001111 ... （每4位一组，低4位为1）
    static const int m3 = 0x00ff00ff;  // 二进制：0000000011111111 0000000011111111 （每8位一组）
    
    uint32_t reverseBits(uint32_t n) {
        // 第1步：交换所有相邻的1位（奇偶位互换）
        n = (n >> 1 & m0) | ((n & m0) << 1);

        // 第2步：以2位为单位，交换相邻的2位组
        n = (n >> 2 & m1) | ((n & m1) << 2);

        // 第3步：以4位为单位，交换相邻的4位组
        n = (n >> 4 & m2) | ((n & m2) << 4);

        // 第4步：以8位为单位，交换相邻的8位组（即字节交换）
        n = (n >> 8 & m3) | ((n & m3) << 8);

        // 第5步：交换高16位和低16位
        return (n >> 16) | (n << 16);
    }
};
```

