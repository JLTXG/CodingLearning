[45. 跳跃游戏 II - 力扣（LeetCode）](https://leetcode.cn/problems/jump-game-ii/description/?envType=problem-list-v2&envId=greedy)

>标签：贪心  |  数组  |  动态规划

# 一、题目描述

给定一个长度为 `n` 的 **0 索引**整数数组 `nums`。初始位置在下标 0。

每个元素 `nums[i]` 表示从索引 `i` 向后跳转的最大长度。换句话说，如果你在索引 `i` 处，你可以跳转到任意 `(i + j)` 处：

- `0 <= j <= nums[i]` 且
- `i + j < n`

返回到达 `n - 1` 的最小跳跃次数。测试用例保证可以到达 `n - 1`。

 

**示例 1:**

```
输入: nums = [2,3,1,1,4]
输出: 2
解释: 跳到最后一个位置的最小跳跃数是 2。
     从下标为 0 跳到下标为 1 的位置，跳 1 步，然后跳 3 步到达数组的最后一个位置。
```

**示例 2:**

```
输入: nums = [2,3,0,1,4]
输出: 2
```

 

**提示:**

- `1 <= nums.length <= 104`
- `0 <= nums[i] <= 1000`
- 题目保证可以到达 `n - 1`



# 二、解题思路\解题过程

这道题采用贪心的思想，先定义三个值，`current`表示当前索引到的、可以跳转到的最大位置，`next`表示下一个能跳转到的最大位置，`result`表示最小跳跃次数，上述三个定义的值都为0，然后接下来我们需要去遍历题目给的数组，`next`的值需要取当前索引范围里的最大值，当遍历到的位置与`current`相等时（并且当前的遍历位置不是数组的最后一个位置），说明当前索引范围已经到头了，接下来就需要去“跳转”了，这个时候`result`就需要加`1`了，然后在更新`current`——将上一个索引范围内记录的`next`值赋予`current`。然后一直遍历下去，直到`current`的值`大于等于`数组的最后一个下标值即可跳出循环（当然也可以不加这一段逻辑，直到遍历完整个数组为止！）
代码如下：

```java
class Solution {
    public int jump(int[] nums) {
        //  current表示当前索引到的、可以跳转到的最大位置，next表示下一个能跳转到的最大位置，result表示跳了几次
        int current = 0, next = 0, result = 0, n = nums.length;
        for(int i = 0; i < n; i++){
            next = (i + nums[i]) > next ? (i + nums[i]) : next;
            if(i == current){
                if(i != n - 1){
                    result++;
                    current = next;
                }
                if(current >= n - 1){
                    break;
                }
            }
        }
        return result;
    }
}
```

# 三、代码演示

## Java代码

```java
class Solution {
    public int jump(int[] nums) {
        //  current表示当前索引到的、可以跳转到的最大位置，next表示下一个能跳转到的最大位置，result表示跳了几次
        int current = 0, next = 0, result = 0, n = nums.length;
        for(int i = 0; i < n; i++){
            next = (i + nums[i]) > next ? (i + nums[i]) : next;
            if(i == current){
                if(i != n - 1){
                    result++;
                    current = next;
                }
                if(current >= n - 1){
                    break;
                }
            }
        }
        return result;
    }
}
```



## Python代码

```python
class Solution:
    def jump(self, nums: List[int]) -> int:
        current, num_next, result, n =0, 0, 0, len(nums)
        for i in range(n):
            num_next = num_next if num_next > i + nums[i] else i + nums[i]
            if i == current :
                if i != n - 1 :
                    result += 1
                    current = num_next
                if current >= n - 1 :
                    break
        return result
```



## C++代码

```C++
class Solution {
public:
    int jump(vector<int>& nums) {
        //  current表示当前索引到的、可以跳转到的最大位置，next表示下一个能跳转到的最大位置，result表示跳了几次
        int current = 0, next = 0, result = 0, n = size(nums);
        for(int i = 0; i < n; i++){
            next = (i + nums[i]) > next ? (i + nums[i]) : next;
            if(i == current){
                if(i != n - 1){
                    result++;
                    current = next;
                }
                if(current >= n - 1){
                    break;
                }
            }
        }
        return result;
    }
};
```

