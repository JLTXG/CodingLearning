本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[1895. 最大的幻方 - 力扣（LeetCode）](https://leetcode.cn/problems/largest-magic-square/description/?envType=daily-question&envId=2026-01-18)

>标签：数组  |  矩阵  |  前缀和

# 一、题目描述

## **问题概括**

给定一个 `m x n` 的整数矩阵 `grid`，矩阵中的元素均为正整数。一个 `k x k` 的子矩阵被称为 **幻方**，当且仅当该子矩阵满足以下条件：

- 所有 `k` 行的元素和相等
- 所有 `k` 列的元素和相等
- 两条主对角线（从左上到右下、从右上到左下）的元素和与行和、列和相等

幻方中的元素可以重复。请你在矩阵 `grid` 中找出 **最大幻方** 的尺寸（即最大的满足上述条件的正方形子矩阵的边长 `k`）。

## **示例说明**

- **示例1**：在给定的 `4×5` 矩阵中，最大的幻方边长为 `3`，其行和、列和以及对角线和均为 `12`。

    ![img](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/13_%E5%89%8D%E7%BC%80%E5%92%8C/Images/1895_%E6%9C%80%E5%A4%A7%E7%9A%84%E5%B9%BB%E6%96%B9_%E7%A4%BA%E4%BE%8B1.jpg)

    ```
    输入：grid = [[7,1,4,5,6],[2,5,1,6,4],[1,5,4,3,2],[1,2,7,3,4]]
    输出：3
    解释：最大幻方尺寸为 3 。
    每一行，每一列以及两条对角线的和都等于 12 。
    - 每一行的和：5+1+6 = 5+4+3 = 2+7+3 = 12
    - 每一列的和：5+5+2 = 1+4+7 = 6+3+3 = 12
    - 对角线的和：5+4+3 = 6+4+2 = 12
    ```

- **示例2**：在给定的 `3×4` 矩阵中，最大的幻方边长为 `2`。

    ![img](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/13_%E5%89%8D%E7%BC%80%E5%92%8C/Images/1895_%E6%9C%80%E5%A4%A7%E7%9A%84%E5%B9%BB%E6%96%B9_%E7%A4%BA%E4%BE%8B2.jpg)

    ```
    输入：grid = [[5,1,3,1],[9,3,3,1],[1,3,3,8]]
    输出：2
    ```

## **数据范围**

- 矩阵的行数 `m` 和列数 `n` 在 `1` 到 `50` 之间。
- 矩阵中的每个元素在 `1` 到 `10^6` 之间。



# 二、解题思路\解题过程

日常没有思路，以下思路参考灵神的题解——[1895. 最大的幻方 - 力扣（LeetCode）](https://leetcode.cn/problems/largest-magic-square/solutions/825422/go-qian-zhui-he-by-endlesscheng-59wj/?envType=daily-question&envId=2026-01-18)——具体如下：

## ✅ 核心思想

- **暴力枚举所有可能的正方形子矩阵**（从大到小尝试，一旦找到就返回，保证最大）
- 使用 **前缀和优化** 快速计算任意行、列、对角线的和
- 判断是否满足魔法方阵条件

---

## 📌 前缀和数组说明

1. **`row_sum[i][j]`**  
    表示第 `i` 行前 `j` 个元素的和（`j` 从 0 开始，所以长度为 `n+1`）  
    → 用于快速计算某行 `[l, r)` 区间的和：`row_sum[i][r] - row_sum[i][l]`
2. **`col_sum[i][j]`**  
    表示第 `j` 列前 `i` 个元素的和（`i` 从 0 开始，所以长度为 `m+1`）  
    → 计算某列 `[top, bottom)` 的和：`col_sum[bottom][j] - col_sum[top][j]`
3. **`diag_sum[i][j]`**  
    表示从 `(0,0)` 到 `(i-1, j-1)` 的主对角线方向（↘）的“累积和”  
    → 主对角线和 = `diag_sum[i][j] - diag_sum[i-k][j-k]`
4. **`anti_sum[i][j]`**  
    表示从右上往左下的反对角线（↙）方向的前缀和
    
    > 注意：`anti_sum[i+1][j] = anti_sum[i][j+1] + grid[i][j]`  
    > → 反对角线和 = `anti_sum[i][j - k] - anti_sum[i - k][j]`
    

> 💡 技巧：`anti_sum` 的递推是“向左下走”，所以当前 `(i,j)` 的值来自 `(i-1, j+1)`

---

## 🔄 枚举逻辑

- 从最大可能的 `k = min(m, n)` 开始往下试（贪心，第一个满足的就是答案）
- 对每个 `k`，遍历所有可能的 `k x k` 子矩阵：
    - 左上角为 `(i - k, j - k)`
    - 右下角为 `(i - 1, j - 1)`
    - 其中 `i ∈ [k, m]`, `j ∈ [k, n]`（因为用了前缀和，索引偏移了）
- 先算主对角线和 `s`
- 再验证：
    - 反对角线和 == `s`
    - 所有 `k` 行的和 == `s`
    - 所有 `k` 列的和 == `s`
- 一旦满足，直接 `return k`



# 三、代码演示

## Java代码

```Java
class Solution {
    public int largestMagicSquare(int[][] grid) {
        int m = grid.length, n = grid[0].length;

        // 行前缀和：rowSum[i][j] = 第 i 行前 j 个元素之和
        int[][] rowSum = new int[m][n + 1];
        // 列前缀和：colSum[i][j] = 第 j 列前 i 个元素之和
        int[][] colSum = new int[m + 1][n];
        // 主对角线（↘）前缀和
        int[][] diagSum = new int[m + 1][n + 1];
        // 反对角线（↙）前缀和
        int[][] antiSum = new int[m + 1][n + 1];

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                int x = grid[i][j];
                rowSum[i][j + 1] = rowSum[i][j] + x;
                colSum[i + 1][j] = colSum[i][j] + x;
                diagSum[i + 1][j + 1] = diagSum[i][j] + x;
                antiSum[i + 1][j] = antiSum[i][j + 1] + x;
            }
        }

        // 从大到小枚举边长 k
        for (int k = Math.min(m, n); k >= 1; k--) {
            for (int i = k; i <= m; i++) {
                for (int j = k; j <= n; j++) {
                    // 主对角线和
                    int mainDiag = diagSum[i][j] - diagSum[i - k][j - k];
                    // 反对角线和
                    int antiDiag = antiSum[i][j - k] - antiSum[i - k][j];

                    if (antiDiag != mainDiag) continue;

                    int target = mainDiag;
                    boolean valid = true;

                    // 检查每一行
                    for (int r = i - k; r < i; r++) {
                        if (rowSum[r][j] - rowSum[r][j - k] != target) {
                            valid = false;
                            break;
                        }
                    }
                    if (!valid) continue;

                    // 检查每一列
                    for (int c = j - k; c < j; c++) {
                        if (colSum[i][c] - colSum[i - k][c] != target) {
                            valid = false;
                            break;
                        }
                    }

                    if (valid) return k;
                }
            }
        }

        return 1;
    }
}
```

![1895_最大的幻方_Java](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/13_%E5%89%8D%E7%BC%80%E5%92%8C/Images/1895_%E6%9C%80%E5%A4%A7%E7%9A%84%E5%B9%BB%E6%96%B9_Java.png)

## Python代码

```Python
class Solution:
    def largestMagicSquare(self, grid: List[List[int]]) -> int:
        m, n = len(grid), len(grid[0])

        # 行前缀和：row_sum[i][j] = 第 i 行前 j 个元素之和
        row_sum = [[0] * (n + 1) for _ in range(m)]
        # 列前缀和：col_sum[i][j] = 第 j 列前 i 个元素之和
        col_sum = [[0] * n for _ in range(m + 1)]
        # 主对角线（↘）前缀和
        diag_sum = [[0] * (n + 1) for _ in range(m + 1)]
        # 反对角线（↙）前缀和
        anti_sum = [[0] * (n + 1) for _ in range(m + 1)]

        # 构建四个前缀和数组
        for i in range(m):
            for j in range(n):
                x = grid[i][j]
                row_sum[i][j + 1] = row_sum[i][j] + x
                col_sum[i + 1][j] = col_sum[i][j] + x
                diag_sum[i + 1][j + 1] = diag_sum[i][j] + x
                # anti_sum[i + 1][j] 来自左上方（实际是右上往左下）
                anti_sum[i + 1][j] = anti_sum[i][j + 1] + x

        # 从最大可能的 k 开始向下枚举
        for k in range(min(m, n), 0, -1):
            # 枚举所有 k x k 子矩阵的右下角 (i - 1, j - 1)，对应前缀和索引 (i, j)
            for i in range(k, m + 1):
                for j in range(k, n + 1):
                    # 主对角线和：从 (i - k, j - k) 到 (i - 1, j - 1)
                    main_diag = diag_sum[i][j] - diag_sum[i - k][j - k]

                    # 反对角线和：从 (i - k, j - 1) 到 (i - 1, j - k)
                    anti_diag = anti_sum[i][j - k] - anti_sum[i - k][j]

                    if anti_diag != main_diag:
                        continue

                    target = main_diag

                    # 检查每一行的和是否等于 target
                    valid = True
                    for r in range(i - k, i):
                        if row_sum[r][j] - row_sum[r][j - k] != target:
                            valid = False
                            break
                    if not valid:
                        continue

                    # 检查每一列的和是否等于 target
                    for c in range(j - k, j):
                        if col_sum[i][c] - col_sum[i - k][c] != target:
                            valid = False
                            break
                    if valid:
                        return k

        return 1  # 至少 1x1 总是魔法方阵
```



## C++代码

```C++
class Solution {
public:
    int largestMagicSquare(vector<vector<int>>& grid) {
        int m = grid.size(), n = grid[0].size();

        // 行前缀和：rowSum[i][j] = 第 i 行前 j 个元素之和
        vector<vector<int>> rowSum(m, vector<int>(n + 1, 0));
        // 列前缀和：colSum[i][j] = 第 j 列前 i 个元素之和
        vector<vector<int>> colSum(m + 1, vector<int>(n, 0));
        // 主对角线（↘）前缀和
        vector<vector<int>> diagSum(m + 1, vector<int>(n + 1, 0));
        // 反对角线（↙）前缀和
        vector<vector<int>> antiSum(m + 1, vector<int>(n + 1, 0));

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                int x = grid[i][j];
                rowSum[i][j + 1] = rowSum[i][j] + x;
                colSum[i + 1][j] = colSum[i][j] + x;
                diagSum[i + 1][j + 1] = diagSum[i][j] + x;
                antiSum[i + 1][j] = antiSum[i][j + 1] + x;
            }
        }

        // 从大到小枚举边长 k
        for (int k = min(m, n); k >= 1; k--) {
            for (int i = k; i <= m; i++) {
                for (int j = k; j <= n; j++) {
                    // 主对角线和
                    int mainDiag = diagSum[i][j] - diagSum[i - k][j - k];
                    // 反对角线和
                    int antiDiag = antiSum[i][j - k] - antiSum[i - k][j];

                    if (antiDiag != mainDiag) continue;

                    int target = mainDiag;
                    bool valid = true;

                    // 检查每一行
                    for (int r = i - k; r < i; r++) {
                        if (rowSum[r][j] - rowSum[r][j - k] != target) {
                            valid = false;
                            break;
                        }
                    }
                    if (!valid) continue;

                    // 检查每一列
                    for (int c = j - k; c < j; c++) {
                        if (colSum[i][c] - colSum[i - k][c] != target) {
                            valid = false;
                            break;
                        }
                    }

                    if (valid) return k;
                }
            }
        }

        return 1;
    }
};
```

