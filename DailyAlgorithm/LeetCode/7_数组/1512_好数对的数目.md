[1512. 好数对的数目 - 力扣（LeetCode）](https://leetcode.cn/problems/number-of-good-pairs/description/?envType=study-plan-v2&envId=primers-list)

>标签：数组  |  哈希表  |  数学  |  计数

# 一、题目描述

给你一个整数数组 `nums` 。

如果一组数字 `(i,j)` 满足 `nums[i]` == `nums[j]` 且 `i` < `j` ，就可以认为这是一组 **好数对** 。

返回好数对的数目。

 

**示例 1：**

```
输入：nums = [1,2,3,1,1,3]
输出：4
解释：有 4 组好数对，分别是 (0,3), (0,4), (3,4), (2,5) ，下标从 0 开始
```

**示例 2：**

```
输入：nums = [1,1,1,1]
输出：6
解释：数组中的每组数字都是好数对
```

**示例 3：**

```
输入：nums = [1,2,3]
输出：0
```

 

**提示：**

- `1 <= nums.length <= 100`
- `1 <= nums[i] <= 100`



# 二、解题思路\解题过程

这道题目也是一道简单的题目，可以有两种解法，这道题按照正常的思路写就OK了，没有什么套路在里面，如下：

- 第一种：采用“桶排序”的思想来求解，代码如下：
```java
class Solution {
    public int numIdenticalPairs(int[] nums) {
        int result = 0, n = nums.length;
        int[] arr = new int[101];
        for (int i = 0; i < n; i++) {
            arr[nums[i]]++;
        }
        for (int i = 1; i < 101; i++){
            if(arr[i] > 1){
                for(int j = 1; j < arr[i]; j++){
                    result += j;
                }
            }
        }
        return result;
    }
}
```
- 第二种：直接暴力求解：
```cpp
class Solution {
public:
    int numIdenticalPairs(vector<int>& nums) {
        // int result = 0, n = size(nums);
        // vector<int> arr(101);
        // for (int i = 0; i < n; i++) {
        //     arr[nums[i]]++;
        // }
        // for (int i = 1; i < 101; i++){
        //     if(arr[i] > 1){
        //         for(int j = 1; j < arr[i]; j++){
        //             result += j;
        //         }
        //     }
        // }
        // return result;
        int result = 0, n = size(nums);
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++){
                if (nums[i] == nums[j] && i < j) {
                    result++;
                }
            }
        }
        return result;
    }
};
```

# 三、代码演示

## Java代码

```java
// 桶排序思想
class Solution {
    public int numIdenticalPairs(int[] nums) {
        int result = 0, n = nums.length;
        int[] arr = new int[101];
        for (int i = 0; i < n; i++) {
            arr[nums[i]]++;
        }
        for (int i = 1; i < 101; i++){
            if(arr[i] > 1){
                for(int j = 1; j < arr[i]; j++){
                    result += j;
                }
            }
        }
        return result;
    }
}

// 暴力求解
class Solution {
    public int numIdenticalPairs(int[] nums) {
        int result = 0, n = nums.length;
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++){
                if (nums[i] == nums[j] && i < j) {
                    result++;
                }
            }
        }
        return result;
    }
}
```

![1512_好数对的数目_Java_桶排序思想](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/7_%E6%95%B0%E7%BB%84/Images/1512_%E5%A5%BD%E6%95%B0%E5%AF%B9%E7%9A%84%E6%95%B0%E7%9B%AE_Java_%E6%A1%B6%E6%8E%92%E5%BA%8F%E6%80%9D%E6%83%B3.png)

## Python代码

```python
# 桶排序思想
class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        result, n = 0, len(nums)
        arr = [0] * 101
        for i in range(n):
            arr[nums[i]] += 1
        for i in range(1, 101):
            if arr[i] > 1 :
                for j in range(1, arr[i]):
                    result += j
        return result
    
# 暴力求解
class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        result, n = 0, len(nums)
        for i in range(n):
            for j in range(i + 1, n):
                if nums[i] == nums[j] and i < j :
                    result += 1
        return result
```



## C++代码

```C++
// 桶排序思想
class Solution {
public:
    int numIdenticalPairs(vector<int>& nums) {
        int result = 0, n = size(nums);
        vector<int> arr(101);
        for (int i = 0; i < n; i++) {
            arr[nums[i]]++;
        }
        for (int i = 1; i < 101; i++){
            if(arr[i] > 1){
                for(int j = 1; j < arr[i]; j++){
                    result += j;
                }
            }
        }
        return result;
    }
};

// 暴力求解
class Solution {
public:
    int numIdenticalPairs(vector<int>& nums) {
        int result = 0, n = size(nums);
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++){
                if (nums[i] == nums[j] && i < j) {
                    result++;
                }
            }
        }
        return result;
    }
};
```

![1512_好数对的数目_C++_暴力求解](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/7_%E6%95%B0%E7%BB%84/Images/1512_%E5%A5%BD%E6%95%B0%E5%AF%B9%E7%9A%84%E6%95%B0%E7%9B%AE_C++_%E6%9A%B4%E5%8A%9B%E6%B1%82%E8%A7%A3.png)