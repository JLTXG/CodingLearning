本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[3047. 求交集区域内的最大正方形面积 - 力扣（LeetCode）](https://leetcode.cn/problems/find-the-largest-area-of-square-inside-two-rectangles/description/?envType=daily-question&envId=2026-01-17)

>标签：几何  |  数组  |  数学

# 一、题目描述

## **问题概括**

在二维平面直角坐标系（x 轴向右为正，y 轴向上为正）上，给定 `n` 个与坐标轴平行的矩形。每个矩形 `i` 由其左下角坐标 `(bottomLeft[i][0], bottomLeft[i][1])` 和右上角坐标 `(topRight[i][0], topRight[i][1])` 唯一定义。

你可以选择其中任意多个（至少两个）矩形，考虑它们共同覆盖的区域（即它们的**交集**）。你的任务是，在所有可能由至少两个矩形的交集构成的区域中，找出能够完全放入该区域内部的**最大正方形**的面积，并返回这个最大可能的面积值。如果任意两个或多个矩形之间都不存在公共交集区域，则返回 `0`。

## **示例核心说明**

- **示例1**：三个矩形中，矩形0和矩形1的交集区域、以及矩形1和矩形2的交集区域，都可以容纳一个边长为1的正方形，面积为1。更大的正方形无法放入，因此答案为1。

    ![img](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/7_%E6%95%B0%E7%BB%84/Images/3047_%E6%B1%82%E4%BA%A4%E9%9B%86%E5%8C%BA%E5%9F%9F%E5%86%85%E7%9A%84%E6%9C%80%E5%A4%A7%E6%AD%A3%E6%96%B9%E5%BD%A2%E9%9D%A2%E7%A7%AF_%E7%A4%BA%E4%BE%8B1.png)

- **示例2**：三个矩形之间存在多种交集组合（两两或三个一起），这些交集区域所能容纳的最大正方形边长也是1，面积为1。

    ![img](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/7_%E6%95%B0%E7%BB%84/Images/3047_%E6%B1%82%E4%BA%A4%E9%9B%86%E5%8C%BA%E5%9F%9F%E5%86%85%E7%9A%84%E6%9C%80%E5%A4%A7%E6%AD%A3%E6%96%B9%E5%BD%A2%E9%9D%A2%E7%A7%AF_%E7%A4%BA%E4%BE%8B2.png)

- **示例3**：给定的三个矩形彼此之间没有公共区域，因此答案为0。

    ![img](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/7_%E6%95%B0%E7%BB%84/Images/3047_%E6%B1%82%E4%BA%A4%E9%9B%86%E5%8C%BA%E5%9F%9F%E5%86%85%E7%9A%84%E6%9C%80%E5%A4%A7%E6%AD%A3%E6%96%B9%E5%BD%A2%E9%9D%A2%E7%A7%AF_%E7%A4%BA%E4%BE%8B3.png)

## **数据范围**

- 矩形数量 `n` 满足 `2 <= n <= 1000`。
- 所有坐标值均为正整数且在 `1` 到 `10^7` 之间。
- 对于每个矩形，左下角的 x、y 坐标严格小于右上角的对应坐标。



# 二、解题思路\解题过程

这是今天的每日一题，看完题目之后完全没有思路，之后去看了灵神的题解——[3047. 求交集区域内的最大正方形面积 - 力扣（LeetCode）](https://leetcode.cn/problems/find-the-largest-area-of-square-inside-two-rectangles/solutions/2653554/jian-ji-xie-fa-wu-xu-fen-lei-tao-lun-pyt-b7yq/?envType=daily-question&envId=2026-01-17)——发现就是一道枚举暴力的题目，不清楚自己为啥一开始没思路，可能是刷的题目不够多，也可能是没有形成系统的思维方式，任重道远呀！
## 优化前

这是一道暴力枚举的题目，需要我们找出矩形之间最大的正方形面积，由题可知，两个矩形之间的交集区域也肯定是矩形（在本题中是这样的），所以，我们只需要对题目提供的矩形进行两两比对求对应的交集——循环枚举——即可！

求出两个矩形之间的交集区域的左下角和右上角的方法如下：
- 左下角横坐标：两个矩形左下角横坐标的最大值
- 左下角纵坐标：两个矩形左下角纵坐标的最大值
- 右上角横坐标：两个矩形右上角横坐标的最小值
- 右上角纵坐标：两个矩形右上角纵坐标的最小值

然后，求出交集区域矩形的长和宽，求出长和宽中的最小值——该最小值即为交集区域正方形的最大边长，然后不断更新这个边长，循环完之后，将该边长平方即可得到答案（注意类型的大小，防止最终的结果过大产生溢出）！

代码如下：
```python
class Solution:
    def largestSquareArea(self, bottomLeft: List[List[int]], topRight: List[List[int]]) -> int:
        max_side = 0
        for i, ((bx1, by1), (tx1, ty1)) in enumerate(zip(bottomLeft, topRight)):
            for j in range(i):
                bx2, by2 = bottomLeft[j]
                tx2, ty2 = topRight[j]
                width = min(tx1, tx2) - max(bx1, bx2)
                height = min(ty1, ty2) - max(by1, by2)
                side = min(width, height)
                max_side = max(side, max_side)
        return max_side * max_side
```

## 优化后

根据灵神的题解——[3047. 求交集区域内的最大正方形面积 - 力扣（LeetCode）](https://leetcode.cn/problems/find-the-largest-area-of-square-inside-two-rectangles/solutions/2653554/jian-ji-xie-fa-wu-xu-fen-lei-tao-lun-pyt-b7yq/?envType=daily-question&envId=2026-01-17)——外层循环枚举的矩形，如果其`长或宽 <= maxSide`，那么交集矩形的`长或宽也 <= maxSide`，所以`maxSide`不会变大，无需跑内层循环，直接`continue`。

代码如下：
```cpp
class Solution {
public:
    long long largestSquareArea(vector<vector<int>>& bottomLeft, vector<vector<int>>& topRight) {
        int maxSide = 0, n = bottomLeft.size();
        for (int i = 0; i < n; i++) {
            const vector<int>& bl1 = bottomLeft[i];
            const vector<int>& tr1 = topRight[i];

            // 优化，如果长或宽 <= maxSide，那么交集矩形的长或宽也 <= maxSide，所以 maxSide 不会变大，无需跑内层循环，直接 continue
            if (tr1[0] - bl1[0] <= maxSide || tr1[1] - bl1[1] <= maxSide) {
                continue;
            }
            
            for (int j = 0; j < i; j++) {
                const vector<int>& bl2 = bottomLeft[j];
                const vector<int>& tr2 = topRight[j];
                
                // 计算重叠区域的宽度和高度
                int width = min(tr1[0], tr2[0]) - max(bl1[0], bl2[0]);
                int height = min(tr1[1], tr2[1]) - max(bl1[1], bl2[1]);
                
                // 只有当 width 和 height 都为正时，才有重叠
                if (width > 0 && height > 0) {
                    int side = min(width, height);
                    maxSide = max(maxSide, side);
                }
            }
        }
        return static_cast<long long>(maxSide) * maxSide;
    }
};
```

## 知识补充
### **1、zip用法**

`zip()` 是 Python 的内置函数，用于**将多个可迭代对象（如列表、元组等）“并排”组合起来**，返回一个 **zip 对象**（在 Python 3 中是迭代器），每次从每个可迭代对象中取出一个元素，组成一个元组。

基本语法：
```Python
zip(iterable1, iterable2, ...)
```

示例：
```Python
a = [1, 2, 3]
b = ['x', 'y', 'z']
zipped = zip(a, b)
print(list(zipped))

# 输出: [(1, 'x'), (2, 'y'), (3, 'z')]
```

### 2、解包用法

`((bx, by), (tx, ty))`——这是`Python`的多重解包语法：
- 外层：`pair = (bl, tr)` → `(bl, tr)`
- 内层：`bl = [bx, by]` → 自动解包为`bx, by`
- 同理 `tr = [tx, ty]` → `tx, ty`

所以`for i, ((bx, by), (tx, ty)) in enumerate(zip(bottomLeft, topRight)):`这一行等价于：
```Python
for i in range(len(bottomLeft)):
	bx, by = bottomLeft[i]
	tx, ty = topRight[i]
```

### 3、const用法

在 C++ 代码中：

```cpp
const vector<int>& bl1 = bottomLeft[i];
```

使用 `const vector<int>&`（即**常量引用**）是一种**良好的 C++ 编程实践**，主要有以下三个重要作用：

---

#### ✅ 1. **避免不必要的拷贝（提升性能）**

- `bottomLeft[i]` 的类型是 `vector<int>`。
  
- 如果你写成：
  
    ```cpp
    vector<int> bl1 = bottomLeft[i];  // ❌ 拷贝整个 vector！
    ```
    
    那么会**复制整个 `vector<int>` 的内容**（包括动态分配的内存），时间复杂度 O(k)，k 是坐标数量（这里是 2，但习惯很重要）。
    
- 而用 **引用**：
  
    ```cpp
    const vector<int>& bl1 = bottomLeft[i];  // ✅ 不拷贝，直接“起别名”
    ```
    
    只是创建一个**别名**（类似指针，但更安全），**零拷贝**，效率高。
    

> 即使这里每个 `vector` 只有 2 个 `int`，拷贝开销小，但养成好习惯对处理大对象（如大数组、字符串等）至关重要。

---

#### ✅ 2. **防止意外修改原始数据（安全性）**

- 加上 `const` 表示：**通过这个引用，不能修改原数据**。
  
- 例如，如果你不小心写了：
  
    ```cpp
    bl1[0] = 999;  // ❌ 编译错误！因为 bl1 是 const 引用
    ```
    
    编译器会报错，防止你**无意中修改输入参数** `bottomLeft`。
    
- 这符合函数设计原则：**如果函数不需要修改输入，就应将其视为只读**。
  

> 你的函数只是读取矩形坐标来计算面积，**绝不应该修改 `bottomLeft` 或 `topRight`**，所以用 `const` 是语义正确的。

---

#### ✅ 3. **表达编程意图（代码可读性）**

- 对阅读代码的人（包括未来的你自己）来说，`const vector<int>&` 清晰地传达了：
  
    > “我只需要读取这个 vector，不会改它，而且我不想拷贝它。”
    
- 这是 C++ 社区广泛认可的**最佳实践**，尤其在 LeetCode、工程代码中非常常见。
  

---

#### 🔍 对比三种写法

| 写法                                      | 是否拷贝 | 能否修改原数据       | 推荐度 |
| ----------------------------------------- | -------- | -------------------- | ------ |
| `vector<int> bl1 = bottomLeft[i];`        | ✅ 是     | 可以（但改的是副本） | ⭐      |
| `vector<int>& bl1 = bottomLeft[i];`       | ❌ 否     | ✅ 可以（危险！）     | ⭐⭐     |
| `const vector<int>& bl1 = bottomLeft[i];` | ❌ 否     | ❌ 不可以（安全！）   | ⭐⭐⭐⭐⭐  |

---

#### ✅ 总结

加 `const` 的作用是：

> **高效（避免拷贝） + 安全（禁止修改） + 清晰（表达只读意图）**

这是写出高质量、工业级 C++ 代码的重要细节。



# 三、代码演示

## Java代码

```Java
// 优化前
class Solution {
    public long largestSquareArea(int[][] bottomLeft, int[][] topRight) {
        int maxSide = 0, n = bottomLeft.length;
        for (int i = 0; i < n; i++) {
            int[] bl1 = bottomLeft[i];
            int[] tr1 = topRight[i];
            for (int j = 0; j < i; j++) {
                int[] bl2 = bottomLeft[j];
                int[] tr2 = topRight[j];
                int width = Math.min(tr1[0], tr2[0]) - Math.max(bl1[0], bl2[0]);
                int height = Math.min(tr1[1], tr2[1]) - Math.max(bl1[1], bl2[1]);
                int side = Math.min(width, height);
                maxSide = maxSide > side ? maxSide : side;
            }
        }
        return (long) maxSide * maxSide;
    }
}

// 优化后
class Solution {
    public long largestSquareArea(int[][] bottomLeft, int[][] topRight) {
        int maxSide = 0, n = bottomLeft.length;
        for (int i = 0; i < n; i++) {
            int[] bl1 = bottomLeft[i];
            int[] tr1 = topRight[i];
            // 优化，如果长或宽 <= maxSide，那么交集矩形的长或宽也 <= maxSide，所以 maxSide 不会变大，无需跑内层循环，直接 continue
            if (tr1[0] - bl1[0] <= maxSide || tr1[1] - bl1[1] <= maxSide) {
                continue;
            }
            for (int j = 0; j < i; j++) {
                int[] bl2 = bottomLeft[j];
                int[] tr2 = topRight[j];
                int width = Math.min(tr1[0], tr2[0]) - Math.max(bl1[0], bl2[0]);
                int height = Math.min(tr1[1], tr2[1]) - Math.max(bl1[1], bl2[1]);
                int side = Math.min(width, height);
                maxSide = maxSide > side ? maxSide : side;
            }
        }
        return (long) maxSide * maxSide;
    }
}
```



## Python代码

```Python
# 优化前
class Solution:
    def largestSquareArea(self, bottomLeft: List[List[int]], topRight: List[List[int]]) -> int:
        max_side = 0
        for i, ((bx1, by1), (tx1, ty1)) in enumerate(zip(bottomLeft, topRight)):
            for j in range(i):
                bx2, by2 = bottomLeft[j]
                tx2, ty2 = topRight[j]
                width = min(tx1, tx2) - max(bx1, bx2)
                height = min(ty1, ty2) - max(by1, by2)
                side = min(width, height)
                max_side = max(side, max_side)
        return max_side * max_side

# 优化后
class Solution:
    def largestSquareArea(self, bottomLeft: List[List[int]], topRight: List[List[int]]) -> int:
        max_side = 0
        for i, ((bx1, by1), (tx1, ty1)) in enumerate(zip(bottomLeft, topRight)):
            if tx1 - bx1 <= max_side or ty1 - by1 <= max_side :
                # 优化，如果长或宽 <= max_side，那么交集矩形的长或宽也 <= max_side，所以 max_side 不会变大，无需跑内层循环，直接 continue
                continue
            for j in range(i):
                bx2, by2 = bottomLeft[j]
                tx2, ty2 = topRight[j]
                width = min(tx1, tx2) - max(bx1, bx2)
                height = min(ty1, ty2) - max(by1, by2)
                side = min(width, height)
                max_side = max(side, max_side)
        return max_side * max_side
```



## C++代码

```C++
// 优化前
class Solution {
public:
    long long largestSquareArea(vector<vector<int>>& bottomLeft, vector<vector<int>>& topRight) {
        int maxSide = 0, n = bottomLeft.size();
        for (int i = 0; i < n; i++) {
            const vector<int>& bl1 = bottomLeft[i];
            const vector<int>& tr1 = topRight[i];
            
            for (int j = 0; j < i; j++) {
                const vector<int>& bl2 = bottomLeft[j];
                const vector<int>& tr2 = topRight[j];
                
                // 计算重叠区域的宽度和高度
                int width = min(tr1[0], tr2[0]) - max(bl1[0], bl2[0]);
                int height = min(tr1[1], tr2[1]) - max(bl1[1], bl2[1]);
                
                // 只有当 width 和 height 都为正时，才有重叠
                if (width > 0 && height > 0) {
                    int side = min(width, height);
                    maxSide = max(maxSide, side);
                }
            }
        }
        return static_cast<long long>(maxSide) * maxSide;
    }
};

// 优化后
class Solution {
public:
    long long largestSquareArea(vector<vector<int>>& bottomLeft, vector<vector<int>>& topRight) {
        int maxSide = 0, n = bottomLeft.size();
        for (int i = 0; i < n; i++) {
            const vector<int>& bl1 = bottomLeft[i];
            const vector<int>& tr1 = topRight[i];

            // 优化，如果长或宽 <= maxSide，那么交集矩形的长或宽也 <= maxSide，所以 maxSide 不会变大，无需跑内层循环，直接 continue
            if (tr1[0] - bl1[0] <= maxSide || tr1[1] - bl1[1] <= maxSide) {
                continue;
            }
            
            for (int j = 0; j < i; j++) {
                const vector<int>& bl2 = bottomLeft[j];
                const vector<int>& tr2 = topRight[j];
                
                // 计算重叠区域的宽度和高度
                int width = min(tr1[0], tr2[0]) - max(bl1[0], bl2[0]);
                int height = min(tr1[1], tr2[1]) - max(bl1[1], bl2[1]);
                
                // 只有当 width 和 height 都为正时，才有重叠
                if (width > 0 && height > 0) {
                    int side = min(width, height);
                    maxSide = max(maxSide, side);
                }
            }
        }
        return static_cast<long long>(maxSide) * maxSide;
    }
};
```

![3047_求交集区域内的最大正方形面积_C++](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/7_%E6%95%B0%E7%BB%84/Images/3047_%E6%B1%82%E4%BA%A4%E9%9B%86%E5%8C%BA%E5%9F%9F%E5%86%85%E7%9A%84%E6%9C%80%E5%A4%A7%E6%AD%A3%E6%96%B9%E5%BD%A2%E9%9D%A2%E7%A7%AF_C++.png)