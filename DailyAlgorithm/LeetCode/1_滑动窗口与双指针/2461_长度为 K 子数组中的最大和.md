本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[2461. 长度为 K 子数组中的最大和 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-sum-of-distinct-subarrays-with-length-k/description/)

>标签：数组  |  哈希表  |  滑动窗口

# 一、题目描述

## **问题概括**

给定一个整数数组 `nums` 和一个正整数 `k`，你需要找出所有长度为 `k` 的连续子数组中，满足 **所有元素互不相同** 的那些子数组，并计算它们的元素之和。返回这些和中的 **最大值**；如果不存在这样的子数组，则返回 `0`。

## **示例说明**

- **示例1**：`nums = [1,5,4,2,9,9,9]`, `k = 3`。符合条件的子数组有 `[1,5,4]`（和10）、`[5,4,2]`（和11）、`[4,2,9]`（和15），最大和为 `15`。
- **示例2**：`nums = [4,4,4]`, `k = 3`。唯一的子数组 `[4,4,4]` 中有重复元素，因此无符合条件的子数组，返回 `0`。

## **数据范围**

- `1 <= k <= nums.length <= 10^5`
- `1 <= nums[i] <= 10^5`



# 二、解题思路\解题过程

## 解题思路

这是今天做的第一道“滑动窗口”类的题目，和昨天的第二道很相似，这道题要求的窗口内的数据都不一样，昨天的第二道“滑动窗口”的题目要求窗口内的数据的不同的种类的数值大于等于某个阈值，所以，这道题目就是换一个判断条件即可，其他的没什么需要改的，具体代码如下：

```python
class Solution:
    def maximumSubarraySum(self, nums: List[int], k: int) -> int:
        windowSum, result, n = 0, 0, len(nums)
        freq = defaultdict(int)
        for i in range(n):
            # 入
            # 右端点进入滑动窗口
            windowSum += nums[i]
            freq[nums[i]] += 1

            # 更新答案
            left = i - k + 1
            if left < 0 :
                # 此时左端点还没有进入滑窗
                continue
            if len(freq) == k :
                result = max(result, windowSum)

            # 出
            windowSum -= nums[left]
            freq[nums[left]] -= 1
            if freq[nums[left]] == 0 :
                freq.pop(nums[left])
        
        return result
```



## 📘 Python `defaultdict` 完全笔记

> **所属模块**：`collections`
> **用途**：自动为缺失的键（key）提供默认值的字典（dict 的增强版）

------

### 1、为什么需要 `defaultdict`？

#### ❌ 普通 `dict` 的问题

当你访问一个不存在的 key 时，会抛出 `KeyError`：

```python
d = {}
print(d['missing'])  # KeyError: 'missing'
```

在**计数、分组、累积**等场景中，需要频繁检查 key 是否存在，代码冗长：

```python
# 普通 dict 实现计数（啰嗦）
freq = {}
for word in words:
    if word in freq:
        freq[word] += 1
    else:
        freq[word] = 1
```

#### ✅ `defaultdict` 的优势

- **自动初始化缺失的 key**
- **代码更简洁、可读性更高**
- **避免 `KeyError`**

------

### 2、基本语法与创建方式

#### 🔧 导入

```python
from collections import defaultdict
```

#### 📌 创建

```python
dd = defaultdict(default_factory)
```

- `default_factory`：一个**可调用对象**（函数），用于生成默认值
- 常见工厂函数：
    - `int` → 默认值 `0`
    - `list` → 默认值 `[]`
    - `set` → 默认值 `set()`
    - `str` → 默认值 `''`
    - 自定义函数（见后文）

------

### 3、常用 `default_factory` 示例

| 工厂函数 | 默认值  | 典型用途             |
| -------- | ------- | -------------------- |
| `int`    | `0`     | 计数、求和           |
| `list`   | `[]`    | 分组、收集同类项     |
| `set`    | `set()` | 去重分组             |
| `str`    | `''`    | 字符串拼接（较少用） |

#### ✅ 示例 1：计数（最常用）

```python
from collections import defaultdict

words = ['apple', 'banana', 'apple']
freq = defaultdict(int)  # int() == 0
for w in words:
    freq[w] += 1

print(freq)  # defaultdict(<class 'int'>, {'apple': 2, 'banana': 1})
```

#### ✅ 示例 2：分组

```python
names = ['Alice', 'Bob', 'Anna']
by_first = defaultdict(list)  # list() == []
for name in names:
    by_first[name[0]].append(name)

print(dict(by_first))  # {'A': ['Alice', 'Anna'], 'B': ['Bob']}
```

#### ✅ 示例 3：去重分组

```python
edges = 
graph = defaultdict(set)
for u, v in edges:
    graph[u].add(v)

print(dict(graph))  # {'A': {'B', 'C'}, 'B': {'A'}}
```

------

### 4、工作原理（内部机制）

当访问 `dd[key]` 时：

1. 如果 `key` 存在 → 返回对应值

2. 如果 

    ```
    key
    ```

     不存在：

    - 调用 `default_factory()` 生成默认值
    - 将 `key: 默认值` 插入字典
    - 返回该默认值

> 💡 注意：**只有通过 `dd[key]` 访问才会触发默认值创建**
> 使用 `dd.get(key)` 不会创建新 key！

```python
dd = defaultdict(int)
print(dd.get('x'))     # None（不会创建 'x'）
print(dd['x'])         # 0（会创建 'x': 0）
print('x' in dd)       # True
```

------

### 5、自定义 `default_factory`

你可以传入任意可调用对象（函数、lambda、类等）：

#### ✅ 示例：默认值为 -1

```python
dd = defaultdict(lambda: -1)
print(dd['missing'])  # -1
```

#### ✅ 示例：默认值为当前时间

```python
from datetime import datetime
dd = defaultdict(datetime.now)
print(dd['first'])  # 第一次访问时的时间
print(dd['second']) # 第二次访问时的时间（不同！）
```

> ⚠️ 注意：如果 `default_factory` 是有状态的（如 `datetime.now`），每次调用结果可能不同。

------

### 6、重要注意事项 ⚠️

#### 1. **`defaultdict` 仍然是 `dict` 的子类**

- 支持所有 dict 方法：`.keys()`, `.values()`, `.items()`, `len()`, `in` 等
- 可以转换为普通 dict：`dict(dd)`

#### 2. **不会自动删除 key**

即使值变成“空”（如 `0`, `[]`），key 依然存在！

```python
dd = defaultdict(int)
dd['a'] = 0
print(len(dd))  # 1（'a' 仍然存在！）
```

✅ **解决方案**：手动清理

```python
if dd[key] == 0:
    del dd[key]   # 或 dd.pop(key)
```

> 📌 这正是你在滑动窗口题目中必须写 `freq.pop(nums[left])` 的原因！

#### 3. **`default_factory` 不能是 `None`**

```python
dd = defaultdict(None)  # 合法，但行为 like 普通 dict
dd['x']  # 仍会报 KeyError！
```

#### 4. **性能**

- 比普通 dict 略慢（因需调用工厂函数）
- 但在需要频繁初始化的场景下，**整体性能更好**（减少条件判断）

------

### 7、`defaultdict` vs 普通 `dict` vs `dict.setdefault()`

| 方法                | 代码示例                          | 优缺点           |
| ------------------- | --------------------------------- | ---------------- |
| 普通 `dict` + `if`  | `if k in d: d[k]+=1 else: d[k]=1` | 冗长             |
| `dict.setdefault()` | `d.setdefault(k, 0); d[k] += 1`   | 中等简洁         |
| **`defaultdict`**   | `d[k] += 1`                       | **最简洁，推荐** |

> ✅ `setdefault` 示例：
>
> ```python
> d = {}
> d.setdefault('a', []).append(1)
> ```

------

### 8、典型应用场景（LeetCode / 实战）

| 场景               | 推荐用法                                     |
| ------------------ | -------------------------------------------- |
| **滑动窗口计数**   | `defaultdict(int)` + 手动 `pop` 清理         |
| **图的邻接表**     | `defaultdict(list)` 或 `defaultdict(set)`    |
| **单词频次统计**   | `defaultdict(int)`                           |
| **按类别分组数据** | `defaultdict(list)`                          |
| **前缀和索引**     | `defaultdict(list)` 存储多个相同前缀和的位置 |

------



# 三、代码演示

## Java代码

```Java
class Solution {
    public long maximumSubarraySum(int[] nums, int k) { 
        int n = nums.length;
        long windowSum = 0, result = 0;
        Map<Integer, Integer> freq = new HashMap<>();
        for (int i = 0; i < n; i++) {
            // 入
            // 右端点进滑窗
            windowSum += nums[i];
            freq.put(nums[i], freq.getOrDefault(nums[i], 0) + 1);

            // 更新答案
            int left = i - k + 1;
            if (left < 0) {
                // 此时左端点还没有进入滑窗
                continue;
            }
            if (freq.size() == k) {
                result = Math.max(result, windowSum);
            }

            // 出
            windowSum -= nums[left];
            freq.put(nums[left], freq.get(nums[left]) - 1);
            if (freq.get(nums[left]) == 0) {
                // 只有计数为 0 才移除
                freq.remove(nums[left]);
            }
        }
        return result;
    }
}
```



## Python代码

```Python
class Solution:
    def maximumSubarraySum(self, nums: List[int], k: int) -> int:
        windowSum, result, n = 0, 0, len(nums)
        freq = defaultdict(int)
        for i in range(n):
            # 入
            # 右端点进入滑动窗口
            windowSum += nums[i]
            freq[nums[i]] += 1

            # 更新答案
            left = i - k + 1
            if left < 0 :
                # 此时左端点还没有进入滑窗
                continue
            if len(freq) == k :
                result = max(result, windowSum)

            # 出
            windowSum -= nums[left]
            freq[nums[left]] -= 1
            if freq[nums[left]] == 0 :
                freq.pop(nums[left])
        
        return result
```

![2461_长度为 K 子数组中的最大和_Python](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/1_%E6%BB%91%E5%8A%A8%E7%AA%97%E5%8F%A3%E4%B8%8E%E5%8F%8C%E6%8C%87%E9%92%88/Images/2461_%E9%95%BF%E5%BA%A6%E4%B8%BA%20K%20%E5%AD%90%E6%95%B0%E7%BB%84%E4%B8%AD%E7%9A%84%E6%9C%80%E5%A4%A7%E5%92%8C_Python.png)

## C++代码

```C++
class Solution {
public:
    long long maximumSubarraySum(vector<int>& nums, int k) {
        long long windowSum = 0, result = 0;
        int n = nums.size();
        unordered_map<int, int> freq;
        for (int i = 0; i < n; i++) {
            // 入
            // 右端点进入滑窗
            windowSum += nums[i];
            freq[nums[i]]++;

            // 更新答案
            int left = i - k + 1;
            if (left < 0) {
                // 此时左端点还没有进入滑窗
                continue;
            }
            if (freq.size() == k) {
                result = max(result, windowSum);
            }

            // 出
            windowSum -= nums[left];
            freq[nums[left]]--;
            if (freq[nums[left]] == 0) {
                // 只有计数为 0 才移除
                freq.erase(nums[left]);
            }
        }
        return result;
    }
};
```

