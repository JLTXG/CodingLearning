本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[3. 无重复字符的最长子串 - 力扣（LeetCode）](https://leetcode.cn/problems/longest-substring-without-repeating-characters/description/?envType=study-plan-v2&envId=top-100-liked)

>标签：哈希表  |  字符串  |  滑动窗口

# 一、题目描述

## **问题概括**

给定一个字符串 `s`，请找出其中**不包含重复字符**的最长连续子字符串（子串）的长度。

## **示例说明**

- **示例1**：`s = "abcabcbb"`，最长无重复字符子串为 `"abc"`，长度为 3。
- **示例2**：`s = "bbbbb"`，最长无重复字符子串为 `"b"`，长度为 1。
- **示例3**：`s = "pwwkew"`，最长无重复字符子串为 `"wke"`，长度为 3（注意 `"pwke"` 是子序列，不是连续子串）。

## **数据范围**

- `0 <= s.length <= 5 * 10^4`
- `s` 由英文字母、数字、符号和空格组成。



# 二、解题思路\解题过程

## 🔑 核心思想：滑动窗口（Sliding Window） + 哈希表（Hash Map）

### ✅ 为什么用滑动窗口？

- 子串是**连续的** → 适合用左右指针维护一个窗口。
- 要求“无重复” → 需要快速判断字符是否在当前窗口中出现过。
- 暴力枚举所有子串是 O(n²)，而滑动窗口可优化到 **O(n)**。

---

## 🧠 算法思路详解

1. **定义窗口**
   
    - 用两个指针 `left` 和 `right` 表示当前子串的左右边界（闭区间 `[left, right]`）。
    - 初始时 `left = 0`，`right` 从 0 开始向右扩展。
2. **记录字符位置**
   
    - 使用哈希表 `lastSeen`（或字典、unordered_map）记录**每个字符最后一次出现的索引**。
3. **扩展右边界**
   
    - 每次将 `right` 右移一位，处理新字符 `c = s[right]`。
4. **处理重复字符**
   
    - 如果 `c` 已经在 `lastSeen` 中，且其上次位置 `≥ left`（说明在当前窗口内）：
        - 将 `left` 移动到 `lastSeen[c] + 1`，跳过重复字符。
    - ⚠️ 注意：**不能让 `left` 往左移动**，所以必须加判断 `lastSeen[c] >= left`。
5. **更新哈希表和答案**
   
    - 更新 `lastSeen[c] = right`
    - 更新最大长度：`result = max(result, right - left + 1)`
6. **遍历结束，返回结果**
   

---

## 🛠️ 关键实现细节（跨语言通用）

| 步骤                     | 注意事项                                                    |
| ------------------------ | ----------------------------------------------------------- |
| **判断字符是否在窗口中** | 必须同时满足：  <br>① 字符存在；  <br>② 其上次位置 ≥ `left` |
| **移动左边界**           | `left = lastSeen[c] + 1`，不是 `left++`                     |
| **更新哈希表**           | 每次都要更新为最新位置，即使重复                            |
| **初始化**               | `left = 0`, `result = 0`，哈希表为空                        |

---

## 🌰 示例演示（s = "abba"）

| right | char | lastSeen                | left | 窗口 | length |
| ----- | ---- | ----------------------- | ---- | ---- | ------ |
| 0     | 'a'  | {'a':0}                 | 0    | "a"  | 1      |
| 1     | 'b'  | {'a':0,'b':1}           | 0    | "ab" | 2      |
| 2     | 'b'  | 'b' 在窗口 → left=2     | 2    | "b"  | 1      |
| 3     | 'a'  | 'a'=0 < left=2 → 不影响 | 2    | "ba" | **2**  |

✅ 最终结果：2

---

## 📚 三语言哈希表操作速查

| 操作          | Java                      | C++                             | Python        |
| ------------- | ------------------------- | ------------------------------- | ------------- |
| 声明          | `Map<Character, Integer>` | `unordered_map<char, int>`      | `dict`        |
| 判断 key 存在 | `map.containsKey(k)`      | `map.find(k) != map.end()`      | `k in dict`   |
| 获取 value    | `map.get(k)`              | `map[k]`（慎用）或 `it->second` | `dict[k]`     |
| 插入/更新     | `map.put(k, v)`           | `map[k] = v`                    | `dict[k] = v` |

---

## ✅ 总结一句话

> **用滑动窗口维护无重复子串，哈希表记录字符最后位置，遇到重复就跳过，实时更新最大长度。**

---

# 三、代码演示

## Java代码

```Java
class Solution {
    public int lengthOfLongestSubstring(String s) {
        char[] arr = s.toCharArray();
        Map<Character, Integer> lastSeen = new HashMap<>();
        // left为滑动窗口的左边界，result为最长长度
        int left = 0, result = 0, n = arr.length;
        for (int right = 0; right < n; right++) {
            // 如果字符arr[i]之前出现过，并且还在滑动窗口内
            if (lastSeen.containsKey(arr[right]) && lastSeen.get(arr[right]) >= left) {
                // 将滑动窗口的左边界移到重复元素的下一个位置
                left = lastSeen.get(arr[right]) + 1;
            }

            // 更新字符arr[i]的最新位置
            lastSeen.put(arr[right], right);

            // 更新答案
            result = Math.max(result, right - left + 1);
        }
        return result;
    }
}
```



## Python代码

```Python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        last_seen = {}
        left, result, n = 0, 0, len(s)

        for right in range(n):
            char = s[right]
            
            if char in last_seen and last_seen[char] >= left:
                left = last_seen[char] + 1
            
            last_seen[char] = right

            result = max(result, right - left + 1)
        
        return result
```

![3_无重复字符的最长子串_Python](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/1_%E6%BB%91%E5%8A%A8%E7%AA%97%E5%8F%A3%E4%B8%8E%E5%8F%8C%E6%8C%87%E9%92%88/Images/3_%E6%97%A0%E9%87%8D%E5%A4%8D%E5%AD%97%E7%AC%A6%E7%9A%84%E6%9C%80%E9%95%BF%E5%AD%90%E4%B8%B2_Python.png)

## C++代码

```C++
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        unordered_map<char, int> lastSeen;
        int left = 0, result = 0, n = s.size();
        for (int right = 0; right < n; right++) {
            char c = s[right];

            if (lastSeen.find(c) != lastSeen.end() && lastSeen[c] >= left) {
                left = lastSeen[c] + 1;
            }

            lastSeen[c] = right;

            result = max(result, right - left + 1);
        }
        return result;
    }
};
```

