本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[263. 丑数 - 力扣（LeetCode）](https://leetcode.cn/problems/ugly-number/description/?envType=study-plan-v2&envId=primers-list)

>标签：数学

# 一、题目描述

## **问题概括**

给定一个整数 `n`，判断它是否是一个**丑数**。丑数定义为：一个正整数，其质因数只包含 `2`、`3` 和 `5`（即可以被分解为若干个 2、3、5 的乘积）。如果 `n` 符合这一定义，返回 `true`；否则，返回 `false`。

## **示例说明**

- **示例1**：`n = 6`，返回 `true`，因为 `6 = 2 × 3`。
- **示例2**：`n = 1`，通常认为 `1` 是丑数（因为它没有其他质因数）。
- **示例3**：`n = 14`，返回 `false`，因为 `14 = 2 × 7`，包含了质因数 `7`。

## **注意**

- 如果 `n` 不是正整数（即 `n ≤ 0`），则直接返回 `false`，因为丑数定义要求是正整数。

## **数据范围**

- `-2^31 <= n <= 2^31 - 1`



# 二、解题思路\解题过程

这也是一道简单的数学题目，根据题目的意思可以得知，只要给定的数`n`能够被{2， 3， 5}这个组合分解下去，直到最终的值等于`1`，那么这个数就是所谓的“丑数”，所以最终的代码如下：
```cpp
class Solution {
public:
    bool isUgly(int n) {
        if (n <= 0) {
            return false;
        }
        vector<int> arr = {2, 3, 5};
        for (auto i : arr) {
            while (n % i == 0) {
                n /= i;
            }
        }
        return n == 1;
    }
};
```



# 三、代码演示

## Java代码

```Java
class Solution {
    public boolean isUgly(int n) {
        if (n <= 0) {
            return false;
        }
        int[] arr = {2, 3, 5};
        for (int i : arr) {
            while (n % i == 0) {
                n /= i;
            }
        }
        return n == 1;
    }
}
```



## Python代码

```Python
class Solution:
    def isUgly(self, n: int) -> bool:
        if n <= 0 :
            return False
        arr = [2, 3, 5]
        for i in arr :
            while n % i == 0 :
                n //= i
        return n == 1
```



## C++代码

```C++
class Solution {
public:
    bool isUgly(int n) {
        if (n <= 0) {
            return false;
        }
        vector<int> arr = {2, 3, 5};
        for (auto i : arr) {
            while (n % i == 0) {
                n /= i;
            }
        }
        return n == 1;
    }
};
```

![263_丑数_C++](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/10_%E6%95%B0%E5%AD%A6/Images/263_%E4%B8%91%E6%95%B0_C++.png)