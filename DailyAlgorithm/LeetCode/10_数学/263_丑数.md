[263. 丑数 - 力扣（LeetCode）](https://leetcode.cn/problems/ugly-number/description/?envType=study-plan-v2&envId=primers-list)

>标签：数学

# 一、题目描述

**丑数** 就是只包含质因数 `2`、`3` 和 `5` 的 *正* 整数。

给你一个整数 `n` ，请你判断 `n` 是否为 **丑数** 。如果是，返回 `true` ；否则，返回 `false` 。

 

**示例 1：**

```
输入：n = 6
输出：true
解释：6 = 2 × 3
```

**示例 2：**

```
输入：n = 1
输出：true
解释：1 没有质因数。
```

**示例 3：**

```
输入：n = 14
输出：false
解释：14 不是丑数，因为它包含了另外一个质因数 7 。
```

 

**提示：**

- `-231 <= n <= 231 - 1`



# 二、解题思路\解题过程

这也是一道简单的数学题目，根据题目的意思可以得知，只要给定的数`n`能够被{2， 3， 5}这个组合分解下去，直到最终的值等于`1`，那么这个数就是所谓的“丑数”，所以最终的代码如下：
```cpp
class Solution {
public:
    bool isUgly(int n) {
        if (n <= 0) {
            return false;
        }
        vector<int> arr = {2, 3, 5};
        for (auto i : arr) {
            while (n % i == 0) {
                n /= i;
            }
        }
        return n == 1;
    }
};
```



# 三、代码演示

## Java代码

```Java
class Solution {
    public boolean isUgly(int n) {
        if (n <= 0) {
            return false;
        }
        int[] arr = {2, 3, 5};
        for (int i : arr) {
            while (n % i == 0) {
                n /= i;
            }
        }
        return n == 1;
    }
}
```



## Python代码

```Python
class Solution:
    def isUgly(self, n: int) -> bool:
        if n <= 0 :
            return False
        arr = [2, 3, 5]
        for i in arr :
            while n % i == 0 :
                n //= i
        return n == 1
```



## C++代码

```C++
class Solution {
public:
    bool isUgly(int n) {
        if (n <= 0) {
            return false;
        }
        vector<int> arr = {2, 3, 5};
        for (auto i : arr) {
            while (n % i == 0) {
                n /= i;
            }
        }
        return n == 1;
    }
};
```

![263_丑数_C++](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/10_%E6%95%B0%E5%AD%A6/Images/263_%E4%B8%91%E6%95%B0_C++.png)