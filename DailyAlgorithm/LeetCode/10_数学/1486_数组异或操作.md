[1486. 数组异或操作 - 力扣（LeetCode）](https://leetcode.cn/problems/xor-operation-in-an-array/description/?envType=study-plan-v2&envId=primers-list)

>标签：位运算  |  数学

# 一、题目描述

给你两个整数，`n` 和 `start` 。

数组 `nums` 定义为：`nums[i] = start + 2*i`（下标从 0 开始）且 `n == nums.length` 。

请返回 `nums` 中所有元素按位异或（**XOR**）后得到的结果。

 

**示例 1：**

```
输入：n = 5, start = 0
输出：8
解释：数组 nums 为 [0, 2, 4, 6, 8]，其中 (0 ^ 2 ^ 4 ^ 6 ^ 8) = 8 。
     "^" 为按位异或 XOR 运算符。
```

**示例 2：**

```
输入：n = 4, start = 3
输出：8
解释：数组 nums 为 [3, 5, 7, 9]，其中 (3 ^ 5 ^ 7 ^ 9) = 8.
```

**示例 3：**

```
输入：n = 1, start = 7
输出：7
```

**示例 4：**

```
输入：n = 10, start = 5
输出：2
```

 

**提示：**

- `1 <= n <= 1000`
- `0 <= start <= 1000`
- `n == nums.length`



# 二、解题思路\解题过程

这也是一道非常简单的题目，当然如果是通过模拟的思想来做这道题，无非就是把正常的语言文字转化为代码就好了，代码如下：（题解中好像有更为“专业”的解法！）

```java
class Solution {
    public int xorOperation(int n, int start) {
        int result = 0;
        for (int i = 0; i < n; i++) {
            result ^= (start + 2 * i);
        }
        return result;
    }
}
```

# 三、代码演示

## Java代码

```java
class Solution {
    public int xorOperation(int n, int start) {
        int result = 0;
        for (int i = 0; i < n; i++) {
            result ^= (start + 2 * i);
        }
        return result;
    }
}
```

![1486_数组异或操作_Java](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/10_%E6%95%B0%E5%AD%A6/Images/1486_%E6%95%B0%E7%BB%84%E5%BC%82%E6%88%96%E6%93%8D%E4%BD%9C_Java.png)

## Python代码

```Python
class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        result = 0
        for i in range(n) :
            result ^= (start + 2 * i)
        return result
```



## C++代码

```C++
class Solution {
public:
    int xorOperation(int n, int start) {
        int result = 0;
        for (int i = 0; i < n; i++) {
            result ^= (start + 2 * i);
        }
        return result;
    }
};
```

