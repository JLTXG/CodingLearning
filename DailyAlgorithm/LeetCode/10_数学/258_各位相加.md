[258. 各位相加 - 力扣（LeetCode）](https://leetcode.cn/problems/add-digits/description/?envType=study-plan-v2&envId=primers-list)

>标签：数学  |  数论

# 一、题目描述

给定一个非负整数 `num`，反复将各个位上的数字相加，直到结果为一位数。返回这个结果。

 

**示例 1:**

```
输入: num = 38
输出: 2 
解释: 各位相加的过程为：
38 --> 3 + 8 --> 11
11 --> 1 + 1 --> 2
由于 2 是一位数，所以返回 2。
```

**示例 2:**

```
输入: num = 0
输出: 0
```

 

**提示：**

- `0 <= num <= 231 - 1`



# 二、解题思路\解题过程

这是一道简单的题目，两层循环就可以搞定，话不多说，代码如下：
```java
class Solution {
    public int addDigits(int num) {
        while(num >= 10){
            int flag = 0;
            while(num != 0){
                flag += num % 10;
                num /= 10;
            }
            num = flag;
        }
        return num;
    }
}
```

这道题目也可以用数学的思维进行解决，具体数学思维可以参考灵神的题解——[258. 各位相加 - 力扣（LeetCode）](https://leetcode.cn/problems/add-digits/solutions/3008465/o1-zuo-fa-jie-shi-qing-chu-9-shi-zen-yao-wmoo/?envType=study-plan-v2&envId=primers-list)，这里只展示代码了，具体的思路可以去考虑灵神对应的题解信息！

代码如下：
```java
class Solution {
    public int addDigits(int num) {
        // while(num >= 10){
        //     int flag = 0;
        //     while(num != 0){
        //         flag += num % 10;
        //         num /= 10;
        //     }
        //     num = flag;
        // }
        // return num;
        return (num - 1) % 9 + 1;
    }
}
```



# 三、代码演示

## Java代码

```java
// 暴力求解
class Solution {
    public int addDigits(int num) {
        while(num >= 10){
            int flag = 0;
            while(num != 0){
                flag += num % 10;
                num /= 10;
            }
            num = flag;
        }
        return num;
    }
}

// 数学方法
class Solution {
    public int addDigits(int num) {
        return (num - 1) % 9 + 1;
    }
}
```

![258_各位相加_Java](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/10_%E6%95%B0%E5%AD%A6/Images/258_%E5%90%84%E4%BD%8D%E7%9B%B8%E5%8A%A0_Java.png)

## Python代码

```Python
class Solution:
    def addDigits(self, num: int) -> int:
        while num >= 10 :
            flag = 0
            while num :
                flag += num % 10
                num //= 10
            num = flag
        return num
```



## C++代码

```C++
class Solution {
public:
    int addDigits(int num) {
        if(num < 10){
            return num;
        }
        while(num >= 10){
            int flag = 0;
            while(num != 0){
                flag += num % 10;
                num /= 10;
            }
            num = flag;
        }
        return num;
    }
};
```

