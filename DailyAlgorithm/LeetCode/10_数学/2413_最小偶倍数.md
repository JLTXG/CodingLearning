[2413. 最小偶倍数 - 力扣（LeetCode）](https://leetcode.cn/problems/smallest-even-multiple/description/?envType=study-plan-v2&envId=primers-list)

>标签：数学  |  数论

# 一、题目描述

给你一个正整数 `n` ，返回 `2` 和 `n` 的最小公倍数（正整数）。

 

**示例 1：**

```
输入：n = 5
输出：10
解释：5 和 2 的最小公倍数是 10 。
```

**示例 2：**

```
输入：n = 6
输出：6
解释：6 和 2 的最小公倍数是 6 。注意数字会是它自身的倍数。
```

 

**提示：**

- `1 <= n <= 150`

# 二、解题思路\解题过程

这是一道简单题目，就是单纯的数学题目，求得是两个数的最小偶倍数，只不过与求两个数的最小公倍数的区别在于，这道题已经将其中一个数`2`明牌了，通过<mark>辗转相除法</mark>——（又称**欧几里得算法**，Euclidean Algorithm）是一种用于**求两个非负整数的最大公约数**（GCD）的经典高效算法——来求出两个数的最大公约数，然后通过`乘数1 / 最大公约数 * 乘数2`得到两个数的最小偶倍数！代码如下：

```java
class Solution {
    public int smallestEvenMultiple(int n) {
        int a = 2, b = n;
        while(b != 0){
            int tmp = b;
            b = a % b;
            a = tmp;
        }
        return n / a * 2;
    }
}
```

# 三、代码演示

## Java代码

```Java
class Solution {
    public int smallestEvenMultiple(int n) {
        int a = 2, b = n;
        while(b != 0){
            int tmp = b;
            b = a % b;
            a = tmp;
        }
        return n / a * 2;
    }
}
```



## Python代码

```Python
class Solution:
    def smallestEvenMultiple(self, n: int) -> int:
        a, b = 2, n
        while b != 0 :
            tmp = b
            b = a % b
            a = tmp
        return n // a * 2
```



## C++代码

```C++
class Solution {
public:
    int smallestEvenMultiple(int n) {
        int a = 2, b = n;
        while(b != 0){
            int tmp = b;
            b = a % b;
            a = tmp;
        }
        return n / a * 2;
    }
};
```

