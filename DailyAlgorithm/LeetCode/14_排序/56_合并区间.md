本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[56. 合并区间 - 力扣（LeetCode）](https://leetcode.cn/problems/merge-intervals/description/?envType=study-plan-v2&envId=top-100-liked)

>标签：数组  |  排序

# 一、题目描述

## **问题概括**

给定一个区间数组 `intervals`，其中每个区间由一对整数 `[start_i, end_i]` 表示。你的任务是将所有相互重叠的区间合并为一个更大的区间，并最终返回一个互不重叠的区间数组，要求该数组能够覆盖输入中的所有区间。

## **示例说明**

- **示例1**：`intervals = [[1,3],[2,6],[8,10],[15,18]]`，合并重叠后得到 `[[1,6],[8,10],[15,18]]`。
- **示例2**：`intervals = [[1,4],[4,5]]`，区间端点相连视为重叠，合并为 `[[1,5]]`。
- **示例3**：`intervals = [[4,7],[1,4]]`，同样端点相连可合并，得到 `[[1,7]]`。

## **数据范围**

- `1 <= intervals.length <= 10^4`
- `intervals[i].length == 2`
- `0 <= start_i <= end_i <= 10^4`



# 二、解题思路\解题过程

## 🔑 问题描述

给定一组区间（每个区间为 `[start, end]`），**合并所有重叠的区间**，返回不重叠的区间列表。

> 示例：  
> 输入：`[[1,3],[2,6],[8,10],[15,18]]`  
> 输出：`[[1,6],[8,10],[15,18]]`

---

## 🎯 核心思想：**排序 + 贪心合并**

### ✅ 步骤分解：

1. **按左端点排序**
   
    - 将所有区间按照 `start` 升序排列。
    - **目的**：确保遍历时，当前区间只可能与前一个区间重叠，不会“跳过”或“遗漏”。
    - 这是使用**贪心策略的前提**。
2. **一次遍历 + 合并**
   
    - 维护一个结果列表 `result`。
    - 对每个新区间 `interval`：
        - **若 `result` 非空** 且 **最后一个区间的右端点 ≥ 当前区间的左端点** → 说明**重叠**，可以合并。
            - 合并方式：更新最后一个区间的右端点为 `max(旧右端点, 当前右端点)`
        - **否则** → 不重叠，直接将当前区间加入 `result`。
3. **返回结果**
   
    - `result` 即为所有合并后的不重叠区间。

---

## ⏱️ 复杂度分析

| 项目       | 复杂度                                                     |
| ---------- | ---------------------------------------------------------- |
| 时间复杂度 | **O(n log n)** —— 主要由排序决定（`n = intervals.length`） |
| 空间复杂度 | **O(1)**（不计输出空间）或 **O(n)**（计入结果存储）        |

> 💡 合并过程本身是 O(n)，因为每个区间最多被访问一次。

---

## 🛠️ 关键实现细节

| 语言       | 排序写法                                          | 获取末尾元素              | 添加元素                |
| ---------- | ------------------------------------------------- | ------------------------- | ----------------------- |
| **Java**   | `Arrays.sort(..., (a,b)->a[0]-b[0])`              | `list.get(list.size()-1)` | `list.add(...)`         |
| **C++**    | `sort(..., [](auto&a,auto&b){return a[0]<b[0];})` | `result.back()`           | `result.push_back(...)` |
| **Python** | `intervals.sort(key=lambda x: x[0])`              | `result[-1]`              | `result.append(...)`    |

> ✅ 所有语言都遵循相同逻辑，只是语法不同。

---

## ⚠️ 注意事项 & 易错点

1. **必须先排序！**
   
    - 如果不排序，`[8,10]` 和 `[1,3]` 会被误判为不重叠，即使后面出现 `[2,6]` 也无法回溯合并。
2. **合并时只更新右端点**
   
    - 左端点已由排序保证是最小的，无需更新。
    - 右端点取 `max(已有右端点, 当前右端点)`，确保覆盖最远边界。
3. **重叠判断条件：`lastEnd >= currentStart`**
   
    - 注意是 **≥**，因为 `[1,3]` 和 `[3,6]` 是连续/重叠的（题目通常视为可合并）。
4. **避免不必要的拷贝（C++/Java）**
   
    - C++ 中用 `const auto&` 或 `const vector<int>&`
    - Java 中 Lambda 参数本质是引用（对象数组），但逻辑上仍需理解“元素是区间对象”

---

## 📌 一句话总结

> **“先排序，再贪心合并：只要当前区间和结果中最后一个区间重叠，就扩展右边界；否则新开一个区间。”**

---

# 三、代码演示

## Java代码

```Java
class Solution {
    public int[][] merge(int[][] intervals) {
        // 按照左端点从小到大排序
        Arrays.sort(intervals, (p, q) -> p[0] - q[0]);
        List<int[]> list = new ArrayList<>();
        for (int[] tmp : intervals) {
            int n = list.size();
            if (n > 0 && list.get(n - 1)[1] >= tmp[0]) {
                // 可以合并
                list.get(n - 1)[1] = Math.max(list.get(n - 1)[1], tmp[1]);
            } else {
                // 不可以合并，增加新的区间
                list.add(tmp);
            }
        }
        return list.toArray(new int[list.size()][]);
    }
}
```



## Python代码

```Python
class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        intervals.sort(key = lambda x : x[0])
        result = []
        for interval in intervals:
            n = len(result)
            if n > 0 and result[-1][1] >= interval[0]:
                result[-1][1] = max(result[-1][1], interval[1])
            else:
                result.append(interval)
        return result
```



## C++代码

```C++
class Solution {
public:
    vector<vector<int>> merge(vector<vector<int>>& intervals) {
        sort(intervals.begin(), intervals.end(), [](const vector<int>& a, const vector<int>& b) {
            return a[0] < b[0];
        });
        vector<vector<int>> result;
        for (const auto& interval : intervals) {
            int n = result.size();
            if (n > 0 && result.back()[1] >= interval[0]) {
                result.back()[1] = max(result.back()[1], interval[1]);
            } else {
                result.push_back(interval);
            }
        }
        return result;
    }
};
```

![56_合并区间_C++]()