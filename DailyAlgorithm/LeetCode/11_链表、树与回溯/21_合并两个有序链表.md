本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[21. 合并两个有序链表 - 力扣（LeetCode）](https://leetcode.cn/problems/merge-two-sorted-lists/description/?envType=study-plan-v2&envId=top-100-liked)

>标签：递归  |  链表

# 一、题目描述

## **问题概括**

给定两个已按非递减顺序排列的单链表的头节点 `l1` 和 `l2`，请将它们合并为一个新的升序链表并返回。新链表应包含两个链表的所有节点，且节点值保持非递减顺序。

## **示例说明**

- **示例1**：`l1 = [1,2,4]`, `l2 = [1,3,4]`，合并后为 `[1,1,2,3,4,4]`。
- **示例2**：`l1 = []`, `l2 = []`，合并后为空链表 `[]`。
- **示例3**：`l1 = []`, `l2 = [0]`，合并后为 `[0]`。

## **数据范围**

- 每个链表的节点数目范围：`[0, 50]`
- 节点值范围：`-100 <= Node.val <= 100`
- 两个链表均已按非递减顺序排列



# 二、解题思路\解题过程

## ✅ 带详细注释的代码

```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next

class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        # 创建一个虚拟头节点（dummy node）
        # 它的作用是简化边界处理（避免单独处理第一个节点）
        head = ListNode()
        p_head = head  # p_head 是当前合并链表的尾部指针

        # 双指针遍历：只要两个链表都未结束，就继续比较
        while list1 is not None and list2 is not None:
            # 比较两个链表当前节点的值
            if list1.val < list2.val:
                # list1 的当前节点更小，接到合并链表末尾
                p_head.next = list1
                list1 = list1.next  # list1 指针前移
            else:
                # list2 的当前节点更小或相等，接到合并链表末尾
                p_head.next = list2
                list2 = list2.next  # list2 指针前移
            
            # 合并链表的尾指针前移
            p_head = p_head.next

        # 此时至少有一个链表已遍历完
        # 将剩余非空链表直接拼接到合并链表末尾
        # （因为原链表有序，剩余部分天然有序）
        p_head.next = list1 if list1 is not None else list2

        # 返回真正的头节点（跳过虚拟头节点）
        return head.next
```

---

## 🧠 核心解题思想分析

### 🔍 问题目标：

将两个**升序排列**的单链表，**合并为一个新的升序链表**。

> 例如：  
> `list1 = [1,2,4]`，`list2 = [1,3,4]`  
> → 合并后：`[1,1,2,3,4,4]`

---

### 💡 核心思想：**双指针 + 虚拟头节点**

#### 1. **双指针比较**

- 分别用 `list1` 和 `list2` 作为两个链表的遍历指针；
- 每次选择**值较小的节点**接入结果链表；
- 被选中的链表指针前移；
- 重复直到其中一个链表耗尽。

#### 2. **虚拟头节点（Dummy Node）**

- 创建一个不存储有效数据的头节点 `head`；
- 所有新节点都接在 `head` 之后；
- 最终返回 `head.next` 即可；
- **优点**：无需特殊处理第一个节点的插入，代码更简洁、鲁棒。

#### 3. **拼接剩余部分**

- 当一个链表遍历完后，另一个链表剩下的部分**整体有序**；
- 直接将其接到结果链表末尾即可，无需再比较。

---

### 🔄 示例演示

```
list1: 1 → 2 → 4
list2: 1 → 3 → 4
```

| 步骤 | 比较     | 接入                | 结果链表          |
| ---- | -------- | ------------------- | ----------------- |
| 初始 | -        | -                   | dummy → ?         |
| 1    | 1 vs 1   | 接 list2 的 1       | dummy → 1         |
| 2    | 1 vs 3   | 接 list1 的 1       | dummy → 1 → 1     |
| 3    | 2 vs 3   | 接 list1 的 2       | dummy → 1 → 1 → 2 |
| 4    | 4 vs 3   | 接 list2 的 3       | ... → 3           |
| 5    | 4 vs 4   | 接 list2 的 4       | ... → 4           |
| 6    | list2 空 | 拼接 list1 剩余 (4) | ... → 4 → 4       |

最终返回 `dummy.next` → `[1,1,2,3,4,4]`

---

### ⏱️ 复杂度分析：

- **时间复杂度**：O(m + n)  
    —— 每个节点访问一次
- **空间复杂度**：O(1)  
    —— 只用常数个指针，**原地合并**（未创建新节点，仅重连指针）

> ✅ 这是最优解！

---

## 📚 笔记总结

### 🧩 核心思想：

> **双指针 + 虚拟头节点（Dummy Node）**

### ✅ 算法步骤：

1. 创建虚拟头节点 `dummy`，`cur = dummy`；
2. 当 `list1` 和 `list2` 都非空：
    - 比较 `list1.val` 与 `list2.val`；
    - 将较小者接到 `cur.next`；
    - 对应链表指针前移，`cur` 前移；
3. 将非空的剩余链表接到 `cur.next`；
4. 返回 `dummy.next`。

### 🔑 关键技巧：

- **虚拟头节点**：统一插入逻辑，避免空链表判断；
- **原地合并**：只修改 `next` 指针，不新建节点；
- **剩余拼接**：利用“有序”性质，直接连接。

### ⏱️ 复杂度：

- 时间：O(m + n)
- 空间：O(1)

### 💡 延伸应用：

- 归并排序的“合并”步骤；
- 合并 K 个有序链表（LeetCode 23）的基础。

---

# 三、代码演示

## Java代码

```Java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode head = new ListNode();
        ListNode pHead = head;
        while (list1 != null && list2 != null) {
            if (list1.val < list2.val) {
                pHead.next = list1;
                list1 = list1.next;
            } else {
                pHead.next = list2;
                list2 = list2.next;
            }
            pHead = pHead.next;
        }
        pHead.next = list1 != null ? list1 : list2;
        return head.next;
    }
}
```



## Python代码

```Python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        head = ListNode()
        p_head = head
        while list1 != None and list2 != None:
            if list1.val < list2.val:
                p_head.next = list1
                list1 = list1.next
            else:
                p_head.next = list2
                list2 = list2.next
            p_head = p_head.next
        p_head.next = list1 if list1 is not None else list2
        return head.next
```

![21_合并两个有序链表_Python]()

## C++代码

```C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {
        ListNode *head = new ListNode;
        ListNode *pHead = head;
        while (list1 && list2) {
            if (list1->val < list2->val) {
                pHead->next = list1;
                list1 = list1->next;
            } else {
                pHead->next = list2;
                list2 = list2->next;
            }
            pHead = pHead->next;
        }
        pHead->next = !list1 ? list2 : list1;
        return head->next;
    }
};
```

