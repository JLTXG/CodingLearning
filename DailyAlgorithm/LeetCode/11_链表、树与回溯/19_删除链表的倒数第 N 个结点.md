本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[19. 删除链表的倒数第 N 个结点 - 力扣（LeetCode）](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/description/?envType=study-plan-v2&envId=top-100-liked)

>标签：链表  |  双指针

# 一、题目描述

## **问题概括**

给定一个单链表的头节点 `head` 和一个整数 `n`，请删除链表的倒数第 `n` 个节点，并返回删除后链表的头节点。要求尝试使用一趟扫描实现。

## **示例说明**

- 输入 `head = [1,2,3,4,5]`, `n = 2`，输出 `[1,2,3,5]`（删除倒数第2个节点4）。
- 输入 `head = [1]`, `n = 1`，输出 `[]`（删除后链表为空）。
- 输入 `head = [1,2]`, `n = 1`，输出 `[1]`（删除最后一个节点）。

## **数据范围**

- 链表节点数目 `sz` 满足 `1 ≤ sz ≤ 30`。
- 节点值范围 `0 ≤ Node.val ≤ 100`。
- 保证 `1 ≤ n ≤ sz`。

## **进阶要求**

能否在只遍历链表一次的情况下完成删除操作？



# 二、解题思路\解题过程

## ✅ 带详细注释的代码

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        // 创建虚拟头节点（dummy node）
        // 作用：统一处理删除头节点等边界情况
        ListNode* pre = new ListNode();
        pre->next = head;

        // 定义两个指针：start（快指针）、end（慢指针）
        // 初始都指向虚拟头节点
        ListNode* start = pre;
        ListNode* end = pre;

        // 第一步：让 start 先走 n 步
        // 此时 start 与 end 之间相隔 n 个节点
        while (n != 0) {
            start = start->next;
            n--;
        }

        // 第二步：同时移动 start 和 end，直到 start 到达最后一个节点（即 start->next == nullptr）
        // 此时 end 指向的是「要删除节点的前一个节点」
        while (start->next != nullptr) {
            start = start->next;
            end = end->next;
        }

        // 删除倒数第 n 个节点：跳过 end->next
        end->next = end->next->next;

        // 返回真正的头节点（跳过虚拟头节点）
        return pre->next;
    }
};
```

> 💡 注意：虽然 `new ListNode()` 会分配内存，但在 LeetCode 环境中无需手动 `delete`，实际工程中应避免内存泄漏。

---

## 🧠 核心解题思想分析

### 🔍 问题目标：

给定一个链表和整数 `n`，**删除倒数第 `n` 个节点**，并返回修改后的链表头。

> 例如：  
> 链表：`1 → 2 → 3 → 4 → 5`，`n = 2`  
> 删除倒数第 2 个（即 `4`），结果：`1 → 2 → 3 → 5`

---

### 💡 核心思想：**双指针（快慢指针） + 虚拟头节点**

#### 1. **为什么用双指针？**

- 如果先遍历一次得到链表长度 `L`，再走 `L - n` 步，需要两次遍历；
- **双指针技巧**可以在**一次遍历**中定位到目标节点的前驱。

#### 2. **关键观察：**

> 若让快指针 `start` 先走 `n` 步，  
> 然后快慢指针同步前进，  
> 当快指针到达**最后一个节点**时，  
> 慢指针正好在**倒数第 n+1 个节点**（即待删节点的前一个）。

✅ 举例说明（n=2）：

```
初始：pre → 1 → 2 → 3 → 4 → 5
         ↑
        end/start

start 先走 2 步：
pre → 1 → 2 → 3 → 4 → 5
      ↑    ↑
     end  start

同步移动直到 start->next == nullptr：
pre → 1 → 2 → 3 → 4 → 5 → nullptr
           ↑         ↑
          end       start

此时 end 指向 3，其 next 是 4（要删除的节点）
```

#### 3. **为什么用虚拟头节点？**

- 处理**删除头节点**的情况（如 `n = 链表长度`）；
- 保证 `end` 始终能指向“待删节点的前驱”，即使该前驱是虚拟节点；
- 避免单独写 `if (n == length)` 的特判逻辑。

---

### ⏱️ 复杂度分析：

- **时间复杂度**：O(L)  
    —— L 为链表长度，仅遍历一次
- **空间复杂度**：O(1)  
    —— 只用常数个指针

> ✅ 这是最优解！

---

## 📚 笔记总结

### 🧩 核心思想：

> **双指针（快慢指针） + 虚拟头节点（Dummy Node）**

### ✅ 算法步骤：

1. 创建虚拟头节点 `dummy`，`dummy->next = head`；
2. 初始化 `fast = dummy`, `slow = dummy`；
3. `fast` 先走 `n` 步；
4. `fast` 和 `slow` 同步前进，直到 `fast->next == nullptr`；
5. 此时 `slow` 指向待删节点的前驱；
6. 执行 `slow->next = slow->next->next`；
7. 返回 `dummy->next`。

### 🔑 关键技巧：

- **快指针先走 n 步**，建立固定间距；
- **虚拟头节点**统一处理所有边界（包括删除头节点）；
- **只遍历一次**，高效且简洁。

### ⏱️ 复杂度：

- 时间：O(L)
- 空间：O(1)

### 💡 对比其他方法：

- 方法1：两次遍历（先求长度）→ O(2L)，代码更啰嗦；
- 方法2：栈/数组存储 → O(L) 空间；
- **本方法最优**：一次遍历 + O(1) 空间。

---

# 三、代码演示

## Java代码

```Java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {
        ListNode pre = new ListNode();
        pre.next = head;
        ListNode start = pre, end = pre;
        while (n != 0) {
            start = start.next;
            n--;
        }
        while (start.next != null) {
            start = start.next;
            end = end.next;
        }
        end.next = end.next.next;
        return pre.next;
    }
}
```



## Python代码

```Python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
        pre = ListNode()
        pre.next = head
        start = pre
        end = pre
        while n != 0:
            start = start.next
            n -= 1
        while start.next != None:
            start = start.next
            end = end.next
        end.next = end.next.next
        return pre.next
```



## C++代码

```C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        ListNode *pre = new ListNode();
        pre->next = head;
        ListNode *start = pre;
        ListNode *end = pre;
        while (n != 0) {
            start = start->next;
            n--;
        }
        while (start->next != nullptr) {
            start = start->next;
            end = end->next;
        }
        end->next = end->next->next;
        return pre->next;
    }
};
```

![19_删除链表的倒数第 N 个结点_C++]()