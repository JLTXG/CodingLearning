本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[110. 平衡二叉树 - 力扣（LeetCode）](https://leetcode.cn/problems/balanced-binary-tree/description/?envType=daily-question&envId=2026-02-08)

>标签：树  |  深度优先搜索  |  二叉树

# 一、题目描述

## **问题概括**

给定一个二叉树的根节点 `root`，判断该二叉树是否为**平衡二叉树**。平衡二叉树的定义为：对于树中的任意节点，其左子树和右子树的高度差（深度差）的绝对值不超过1，且其左子树和右子树也都是平衡二叉树。

## **示例说明**

- **示例1**：输入 `root = [3,9,20,null,null,15,7]`，输出 `true`。
- **示例2**：输入 `root = [1,2,2,3,3,null,null,4,4]`，输出 `false`。
- **示例3**：输入 `root = []`（空树），输出 `true`。

## **数据范围**

- 树中节点数在 `[0, 5000]` 范围内。
- 节点值在 `-10^4` 到 `10^4` 之间。



# 二、解题思路\解题过程

## 🧩 题目要求回顾

> 给定一棵二叉树的根节点 `root`，判断它是否是 **高度平衡的二叉树**。
>
> **高度平衡定义**：对于树中的 **每一个节点**，其左子树和右子树的高度差的绝对值 **不超过 1**。

✅ 示例：

- `[3,9,20,null,null,15,7]` → 是平衡的
- `[1,2,2,3,3,null,null,4,4]` → 不是平衡的（某节点左右高度差为 2）

---

## ✅ 带详细注释的代码

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */

class Solution {
public:
    // 主函数：判断整棵树是否为平衡二叉树
    bool isBalanced(TreeNode* root) {
        // 调用辅助函数，若返回 -1 表示不平衡；否则返回的是树的高度（≥0）
        return myIsBalanced(root) != -1;
    }

private:
    // 辅助函数：计算以 node 为根的子树的高度
    // 如果该子树是平衡的，则返回其高度（≥0）
    // 如果不平衡，则返回 -1（作为“不平衡”的标记）
    int myIsBalanced(TreeNode* node) {
        // 空节点：高度为 0，且天然平衡
        if (node == nullptr) {
            return 0;
        }

        // 递归获取左子树的高度（或 -1 表示不平衡）
        int mLeft = myIsBalanced(node->left);

        // 递归获取右子树的高度（或 -1 表示不平衡）
        int mRight = myIsBalanced(node->right);

        // 判断当前节点是否“破坏”了平衡性：
        // 情况1：左子树已不平衡（mLeft == -1）
        // 情况2：右子树已不平衡（mRight == -1）
        // 情况3：左右子树都平衡，但高度差 > 1
        if (mLeft == -1 || mRight == -1 || abs(mLeft - mRight) > 1) {
            return -1;  // 当前子树不平衡，向上返回 -1
        }

        // 当前节点平衡，返回该子树的高度 = max(左高, 右高) + 1
        return max(mLeft, mRight) + 1;
    }
};
```

---

## 🔍 核心解题思想分析

### ❌ 低效做法（自顶向下）：

对每个节点都单独计算左右子树高度：

```cpp
int getHeight(TreeNode* node) { ... }
bool isBalanced(TreeNode* root) {
    if (!root) return true;
    return abs(getHeight(root->left) - getHeight(root->right)) <= 1
           && isBalanced(root->left)
           && isBalanced(root->right);
}
```

- **问题**：大量重复计算！每个节点的高度被多次求取。
- **时间复杂度**：最坏 O(n²)（如退化为链表）

---

### ✅ 高效做法（自底向上 + 后序遍历）

#### 关键思想：

> **在一次后序遍历中，同时完成“计算高度”和“判断平衡”两件事**。

#### 为什么有效？

- **后序遍历顺序**：先处理左右子树，再处理当前节点 → 自然知道子树信息
- **信息复用**：子树高度在递归返回时直接传递，无需重复计算
- **剪枝优化**：一旦发现某个子树不平衡（返回 -1），立即向上传播，不再继续无意义的递归

#### 时间复杂度：

- 每个节点仅访问一次 → **O(n)**
- 空间复杂度：**O(h)**（h 为树高，递归调用栈）

---

## 📌 笔记重点总结

### ✅ 方法名称：

> **自底向上的后序遍历 + 哨兵值剪枝**

### ✅ 核心技巧：

1. **一个函数干两件事**：既返回高度，又隐式传递平衡状态
2. **用 -1 作为“不平衡”的哨兵值**，避免额外布尔参数
3. **后序遍历天然适合“子问题先解决”类问题**

### ✅ 返回值含义：

| 返回值 | 含义                     |
| ------ | ------------------------ |
| ≥ 0    | 子树平衡，且值为子树高度 |
| -1     | 子树不平衡               |

### ✅ 类似题型：

- LeetCode 543. 二叉树的直径（返回深度+更新全局答案）
- LeetCode 124. 二叉树中的最大路径和（返回单边最大路径+更新全局）

---

## ✅ 一句话终极笔记

> **判断平衡二叉树应采用“自底向上后序遍历”，用 -1 标记不平衡状态，在 O(n) 时间内一次性完成高度计算与平衡验证。**

---

# 三、代码演示

## Java代码

```Java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean isBalanced(TreeNode root) {
        return myIsBalanced(root) != -1;
    }
    private int myIsBalanced(TreeNode node) {
        if (node == null) {
            return 0;
        }
        int mLeft = myIsBalanced(node.left);
        int mRight = myIsBalanced(node.right);
        if (mLeft == -1 || mRight == -1 || Math.abs(mLeft - mRight) > 1) {
            return -1;
        }
        return Math.max(mLeft, mRight) + 1;
    }
}
```



## Python代码

```Python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def isBalanced(self, root: Optional[TreeNode]) -> bool:
        def myIsBalanced(node: Optional[TreeNode]) -> int:
            if node is None:
                return 0
            n_left = myIsBalanced(node.left)
            n_right = myIsBalanced(node.right)
            if n_left == -1 or n_right == -1 or abs(n_left - n_right) > 1:
                return -1
            return max(n_left, n_right) + 1
        return myIsBalanced(root) != -1
```



## C++代码

```C++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool isBalanced(TreeNode* root) {
        return myIsBalanced(root) != -1;
    }

private:
    int myIsBalanced(TreeNode* node) {
        if (node == nullptr) {
            return 0;
        }
        int mLeft = myIsBalanced(node->left);
        int mRight = myIsBalanced(node->right);
        if (mLeft == -1 || mRight == -1 || abs(mLeft - mRight) > 1) {
            return -1;
        }
        return max(mLeft, mRight) + 1;
    }
};
```

![110_平衡二叉树_C++](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/11_%E9%93%BE%E8%A1%A8%E3%80%81%E6%A0%91%E4%B8%8E%E5%9B%9E%E6%BA%AF/Images/110_%E5%B9%B3%E8%A1%A1%E4%BA%8C%E5%8F%89%E6%A0%91_C++.png)