本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[206. 反转链表 - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-linked-list/description/?envType=study-plan-v2&envId=top-100-liked)

>标签：递归  |  链表

# 一、题目描述

## **问题概括**

给定一个单链表的头节点 `head`，请将该链表反转，并返回反转后链表的头节点。你可以使用迭代或递归的方法实现。

## **示例说明**

- 输入：`head = [1,2,3,4,5]`，输出：`[5,4,3,2,1]`
- 输入：`head = [1,2]`，输出：`[2,1]`
- 输入：`head = []`，输出：`[]`

## **数据范围**

- 链表节点数目范围为 `[0, 5000]`
- 节点值范围为 `-5000` 到 `5000`



# 二、解题思路\解题过程

## ✅ 带详细注释的 C++ 代码

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        // 初始化前驱指针 p 为 nullptr（反转后原头节点的 next 应为 null）
        ListNode* p = nullptr;

        // 遍历原链表，直到 head 变为空（即所有节点都处理完毕）
        while (head) {
            // 1️⃣ 保存当前节点的下一个节点，防止断链后丢失
            ListNode* tmp = head->next;

            // 2️⃣ 将当前节点的 next 指向前驱节点（实现“反转”指向）
            head->next = p;

            // 3️⃣ 移动前驱指针 p 到当前节点（当前节点成为下一轮的“前驱”）
            p = head;

            // 4️⃣ 移动 head 指针到下一个待处理节点（即之前保存的 tmp）
            head = tmp;
        }

        // 循环结束后，p 指向原链表的最后一个节点，也就是新链表的头节点
        return p;
    }
};
```

---

## 🧠 核心解题思想分析

### 🔍 问题目标：

将一个单向链表 **原地反转**，返回新的头节点。

例如：

```
输入：1 → 2 → 3 → 4 → 5 → nullptr  
输出：5 → 4 → 3 → 2 → 1 → nullptr
```

---

### 💡 核心思想：**迭代 + 指针重定向**

我们不能像数组那样直接交换，因为链表靠指针连接。所以要：

- **逐个遍历每个节点**
- **在不丢失后续节点的前提下，把当前节点的 `next` 指向前一个节点**

这就需要 **三个关键角色**（虽然代码中只显式用了两个）：

| 指针   | 作用                                                 |
| ------ | ---------------------------------------------------- |
| `head` | 当前正在处理的节点（也是“游标”）                     |
| `p`    | 已经反转部分的**新头节点**（即当前节点的前一个节点） |
| `tmp`  | 临时保存 `head->next`，防止反转后找不到下一个节点    |

> ✅ 这是一种典型的 **“边遍历边反转”** 的迭代策略。

---

### 🔄 执行过程图解

初始：

```
p = nullptr
head → 1 → 2 → 3 → nullptr
```

**第1轮：**

- `tmp = 2`
- `1 → nullptr` （`head->next = p`）
- `p = 1`
- `head = 2` 
    状态：`p → 1 → nullptr`，`head → 2 → 3`

**第2轮：**

- `tmp = 3`
- `2 → 1`
- `p = 2`
- `head = 3`
    状态：`2 → 1 → nullptr`，`head → 3`

**第3轮：**

- `tmp = nullptr`
- `3 → 2`
- `p = 3`
- `head = nullptr`

循环结束，返回 `p`（即 3），得到：`3 → 2 → 1 → nullptr`

---

### ⏱️ 复杂度分析：

- **时间复杂度**：O(n) —— 每个节点访问一次
- **空间复杂度**：O(1) —— 只用常数个额外指针（原地反转）

---

## 📚 笔记总结

### 🧩 核心思想：

> **迭代法原地反转**：通过维护前驱指针和临时指针，在遍历过程中逐个反转节点的指向。

### ✅ 算法步骤：

1. 初始化 `prev = nullptr`（表示新链表的尾部）；
2. 遍历原链表（`curr = head`）：
    - 保存 `next = curr->next`；
    - 将 `curr->next = prev`（反转指针）；
    - `prev = curr`，`curr = next`（向前移动）；
3. 返回 `prev`（即新头节点）。

### 🔑 关键点：

- **必须先保存 `next`**，否则反转后会丢失后续节点；
- 最终 `prev` 指向原链表最后一个节点，即新头；
- **原地操作，无需额外空间**。

### ⏱️ 复杂度：

- 时间：O(n)
- 空间：O(1)

### 💡 延伸思考：

- 递归写法也存在（但空间 O(n) 因递归栈）；
- 此模式是很多链表题的基础（如反转部分链表、回文链表等）。

---

# 三、代码演示

## Java代码

```Java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode reverseList(ListNode head) {
        ListNode p = new ListNode();
        p = null;
        while (head != null) {
            ListNode tmp = head.next;
            head.next = p;
            p = head;
            head = tmp;
        }
        return p;
    }
}
```



## Python代码

```Python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        p = None
        while head:
            tmp = head.next
            head.next = p
            p = head
            head = tmp
        return p
```



## C++代码

```C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* p = nullptr;
        while (head) {
            ListNode* tmp = head->next;
            head->next = p;
            p = head;
            head = tmp;
        }
        return p;
    }
};
```

![206_反转链表_C++]()