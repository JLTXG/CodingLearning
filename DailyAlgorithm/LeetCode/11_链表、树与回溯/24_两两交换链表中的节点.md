本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[24. 两两交换链表中的节点 - 力扣（LeetCode）](https://leetcode.cn/problems/swap-nodes-in-pairs/description/?envType=study-plan-v2&envId=top-100-liked)

>标签：递归  |  链表

# 一、题目描述

## **问题概括**

给定一个单链表，请你将链表中每两个相邻的节点进行交换，并返回交换后链表的头节点。要求只能通过改变节点之间的链接关系来实现，不能修改节点内部的值。

## **示例说明**

- 输入 `head = [1,2,3,4]`，交换后得到 `[2,1,4,3]`。
- 空链表返回空。
- 只有一个节点时返回原链表。

## **数据范围**

- 节点数目范围 `[0, 100]`
- 节点值范围 `0` 到 `100`



# 二、解题思路\解题过程

## ✅ 带详细注释的代码

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode swapPairs(ListNode head) {
        // 创建哨兵节点（dummy），指向原头节点
        // 作用：统一处理头节点被交换的情况，避免特判
        ListNode dummy = new ListNode(0, head);

        // node0：当前待交换 pair 的前一个节点（初始为 dummy）
        // node1：当前 pair 的第一个节点（初始为 head）
        ListNode node0 = dummy;
        ListNode node1 = head;

        // 循环条件：至少有两个节点可交换（node1 和 node1.next 都非空）
        while (node1 != null && node1.next != null) {
            // 定义四个关键节点（便于理解指针重连）
            // ListNode node1 = 当前 pair 第一个节点（已定义）
            ListNode node2 = node1.next;   // 当前 pair 第二个节点
            ListNode node3 = node2.next;   // 下一对的起始节点（可能为 null）

            // 执行三步指针重连，完成 node1 和 node2 的交换
            node0.next = node2;   // 前驱 → node2（新头）
            node2.next = node1;   // node2 → node1（反转 pair 内部）
            node1.next = node3;   // node1 → 下一对（保持链表连续）

            // 移动指针，准备下一轮交换
            node0 = node1;        // 新的前驱是原 node1（现在在 pair 末尾）
            node1 = node3;        // 下一轮的第一个节点是 node3
        }

        // 返回新链表的真实头节点（跳过哨兵）
        return dummy.next;
    }
}
```

> 💡 注意：虽然变量名 `node0`, `node1`, `node2`, `node3` 是临时命名，但它们清晰表达了局部结构。

---

## 🧠 核心解题思想分析

### 🔍 问题目标：

给定一个链表，**两两交换相邻节点**，并返回修改后的链表头。

> 例如：  
> 输入：`1 → 2 → 3 → 4`  
> 输出：`2 → 1 → 4 → 3`  
> （不能只交换值，必须交换节点本身！）

---

### 💡 核心思想：**迭代 + 哨兵节点 + 局部四节点重连**

#### 1. **为什么用哨兵节点？**

- 当交换发生在**头两个节点**时，链表头会改变；
- 哨兵节点作为**固定前驱**，使所有交换逻辑统一，无需单独处理头节点。

#### 2. **每次交换涉及 4 个节点**

对于每一对 `(A → B)`，其上下文为：

```
... → X → A → B → Y → ...
      ↑    ↑    ↑    ↑
    node0 node1 node2 node3
```

目标是变成：

```
... → X → B → A → Y → ...
```

只需三步重连：

- `X.next = B`
- `B.next = A`
- `A.next = Y`

#### 3. **指针移动规律**

- 每次交换后，`node0` 移到 `A`（现在是 pair 的尾部）；
- `node1` 移到 `Y`（下一对的开头）；
- 循环继续，直到不足两个节点。

---

### 🔄 示例演示

输入：`1 → 2 → 3 → 4 → null`

**初始**：

```
dummy → 1 → 2 → 3 → 4
  ↑      ↑
node0  node1
```

**第1轮**：

- node2 = 2, node3 = 3
- 重连：dummy→2, 2→1, 1→3
- 结果：`dummy → 2 → 1 → 3 → 4`
- 更新：node0 = 1, node1 = 3

**第2轮**：

- node2 = 4, node3 = null
- 重连：1→4, 4→3, 3→null
- 结果：`dummy → 2 → 1 → 4 → 3`
- 更新：node0 = 3, node1 = null → 循环结束

返回 `dummy.next = 2` ✅

---

### ⏱️ 复杂度分析：

- **时间复杂度**：O(n)  
    —— 每个节点访问一次
- **空间复杂度**：O(1)  
    —— 只用常数个指针

> ✅ 这是最优解！

---

## 📚 笔记总结

### 🧩 核心思想：

> **迭代 + 哨兵节点 + 四节点局部重连**

### ✅ 算法步骤：

1. 创建哨兵节点 `dummy`，`dummy.next = head`；
2. 初始化 `prev = dummy`, `curr = head`；
3. 当 `curr != null && curr.next != null`：
    - 设 `first = curr`, `second = curr.next`, `nextPair = second.next`
    - 执行三步重连：
        - `prev.next = second`
        - `second.next = first`
        - `first.next = nextPair`
    - 更新指针：`prev = first`, `curr = nextPair`
4. 返回 `dummy.next`

### 🔑 关键技巧：

- **哨兵节点**避免头节点特判；
- **每次只关注当前 pair 及其前后**（共4节点）；
- **先连后断**：确保不丢失后续链表；
- **指针更新顺序**：先更新连接，再移动指针。

### ⏱️ 复杂度：

- 时间：O(n)
- 空间：O(1)

### 💡 对比递归解法：

- 递归更简洁，但空间 O(n)（调用栈）；
- **本迭代解法更高效**，适合生产环境。

---

# 三、代码演示

## Java代码

```Java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode swapPairs(ListNode head) {
        // 创建哨兵节点（dummy），指向原头节点
        // 作用：统一处理头节点被交换的情况，避免特判
        ListNode dummy = new ListNode(0, head);

        // node0：当前待交换 pair 的前一个节点（初始为 dummy）
        // node1：当前 pair 的第一个节点（初始为 head）
        ListNode node0 = dummy;
        ListNode node1 = head;

        // 循环条件：至少有两个节点可交换（node1 和 node1.next 都非空）
        while (node1 != null && node1.next != null) {
            // 定义四个关键节点（便于理解指针重连）
            // ListNode node1 = 当前 pair 第一个节点（已定义）
            ListNode node2 = node1.next;   // 当前 pair 第二个节点
            ListNode node3 = node2.next;   // 下一对的起始节点（可能为 null）

            // 执行三步指针重连，完成 node1 和 node2 的交换
            node0.next = node2;   // 前驱 → node2（新头）
            node2.next = node1;   // node2 → node1（反转 pair 内部）
            node1.next = node3;   // node1 → 下一对（保持链表连续）

            // 移动指针，准备下一轮交换
            node0 = node1;        // 新的前驱是原 node1（现在在 pair 末尾）
            node1 = node3;        // 下一轮的第一个节点是 node3
        }

        // 返回新链表的真实头节点（跳过哨兵）
        return dummy.next;
    }
}
```



## Python代码

```Python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def swapPairs(self, head: Optional[ListNode]) -> Optional[ListNode]:
        # 创建哨兵节点（dummy），指向原头节点
        # 作用：统一处理头节点被交换的情况，避免特判
        dummy = ListNode(0, head)

        # node0：当前待交换 pair 的前一个节点（初始为 dummy）
        # node1：当前 pair 的第一个节点（初始为 head）
        node0 = dummy
        node1 = head

        # 循环条件：至少有两个节点可交换（node1 和 node1.next 都非空）
        while node1 is not None and node1.next is not None:
            # 定义四个关键节点（便于理解指针重连）
            # node1 = 当前 pair 第一个节点（已定义）
            node2 = node1.next    # 当前 pair 第二个节点
            node3 = node2.next    # 下一对的起始节点（可能为 None）

            # 执行三步指针重连，完成 node1 和 node2 的交换
            node0.next = node2    # 前驱 -> node2（新头）
            node2.next = node1    # node2 -> node1（反转 pair 内部）
            node1.next = node3    # node1 -> 下一对（保持链表连续）

            # 移动指针，准备下一轮交换
            node0 = node1         # 新的前驱是原 node1（现在在 pair 末尾）
            node1 = node3         # 下一轮的第一个节点是 node3

        # 返回新链表的真实头节点（跳过哨兵）
        return dummy.next
```

![24_两两交换链表中的节点_Python]()

## C++代码

```C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* swapPairs(ListNode* head) {
        // 创建哨兵节点（dummy），指向原头节点
        // 作用：统一处理头节点被交换的情况，避免特判
        ListNode* dummy = new ListNode(0, head);

        // node0：当前待交换 pair 的前一个节点（初始为 dummy）
        // node1：当前 pair 的第一个节点（初始为 head）
        ListNode* node0 = dummy;
        ListNode* node1 = head;

        // 循环条件：至少有两个节点可交换（node1 和 node1->next 都非空）
        while (node1 != nullptr && node1->next != nullptr) {
            // 定义四个关键节点（便于理解指针重连）
            // ListNode* node1 = 当前 pair 第一个节点（已定义）
            ListNode* node2 = node1->next;   // 当前 pair 第二个节点
            ListNode* node3 = node2->next;   // 下一对的起始节点（可能为 null）

            // 执行三步指针重连，完成 node1 和 node2 的交换
            node0->next = node2;   // 前驱 -> node2（新头）
            node2->next = node1;   // node2 -> node1（反转 pair 内部）
            node1->next = node3;   // node1 -> 下一对（保持链表连续）

            // 移动指针，准备下一轮交换
            node0 = node1;         // 新的前驱是原 node1（现在在 pair 末尾）
            node1 = node3;         // 下一轮的第一个节点是 node3
        }

        // 返回新链表的真实头节点（跳过哨兵）
        return dummy->next;
    }
};
```

