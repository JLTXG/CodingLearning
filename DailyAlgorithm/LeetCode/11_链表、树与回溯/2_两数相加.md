本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[2. 两数相加 - 力扣（LeetCode）](https://leetcode.cn/problems/add-two-numbers/description/?envType=study-plan-v2&envId=top-100-liked)

>标签：初级工程师  |  递归  |  链表  |  数学

# 一、题目描述

## **问题概括**

给定两个非空的单链表，分别表示两个非负整数。每个链表的节点按 **逆序** 存储数字的每一位（即链头是个位，下一个节点是十位，依此类推）。请计算这两个数的和，并以同样的逆序形式返回一个新的链表，表示它们的和。

## **示例说明**

- 输入：`l1 = [2,4,3]` 表示数字 342，`l2 = [5,6,4]` 表示数字 465，它们的和为 807，逆序表示为 `[7,0,8]`。
- 输入：`l1 = [0]`, `l2 = [0]`，和为 0，返回 `[0]`。
- 输入：`l1 = [9,9,9,9,9,9,9]` (7个9)，`l2 = [9,9,9,9]` (4个9)，和为 10000098？实际逆序结果如示例所示。

## **数据范围**

- 每个链表节点数在 `[1, 100]` 之间。
- 节点值范围 `0` 到 `9`。
- 输入保证除数字 0 外没有前导零。



# 二、解题思路\解题过程

## ✅ 带详细注释的代码

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        // 从头节点开始相加，初始进位为 0
        return addTwo(l1, l2, 0);
    }

    /**
     * 递归函数：合并两个链表当前位置的值，并处理进位
     * @param l1 当前 l1 链表节点（可能为 null）
     * @param l2 当前 l2 链表节点（可能为 null）
     * @param carry 来自低位的进位（0 或 1）
     * @return 新链表的当前节点
     */
    private ListNode addTwo(ListNode l1, ListNode l2, int carry) {
        // 🧩 递归终止条件：
        // 当两个链表都遍历完，且没有进位时，不再创建新节点
        if (l1 == null && l2 == null && carry == 0) {
            return null;
        }

        // 当前位的总和 = 进位 + l1.val（如果有） + l2.val（如果有）
        int sum = carry;

        // 如果 l1 不为空，加上它的值，并移动指针
        if (l1 != null) {
            sum += l1.val;
            l1 = l1.next;
        }

        // 如果 l2 不为空，加上它的值，并移动指针
        if (l2 != null) {
            sum += l2.val;
            l2 = l2.next;
        }

        // 📌 创建当前节点：
        // - 节点值 = sum % 10（个位）
        // - 下一个节点 = 递归处理剩余部分，进位 = sum / 10（十位）
        return new ListNode(sum % 10, addTwo(l1, l2, sum / 10));
    }
}
```

---

## 🧠 核心解题思想分析

### 🔍 问题目标：

给定两个**非空**的链表，表示两个**逆序存储**的非负整数（个位在头），  
将它们**相加**，并以**相同形式**返回结果链表。

> 例如：  
> `l1 = [2,4,3]` → 表示 342  
> `l2 = [5,6,4]` → 表示 465  
> 和 = 807 → 返回 `[7,0,8]`

---

### 💡 核心思想：**模拟加法 + 递归处理进位**

#### 1. **模拟竖式加法**

- 从个位开始（链表头），逐位相加；
- 每一位计算：`sum = digit1 + digit2 + carry_in`
- 当前位结果：`sum % 10`
- 向高位进位：`carry_out = sum / 10`

#### 2. **递归天然匹配链表结构**

- 链表是递归定义的数据结构（`node → node.next`）；
- 递归函数自然地处理“当前位”和“剩余位”；
- 每次递归调用处理下一位，并传递进位。

#### 3. **统一处理不同长度 & 最终进位**

- 若某链表先结束，视为后续位为 0；
- 若最后还有进位（如 5+5=10），会额外生成一个节点；
- 递归终止条件确保不会多建节点。

---

### 🔄 示例演示

输入：`l1 = [9,9,9]`, `l2 = [9,9]`  
→ 表示 999 + 99 = 1098 → 输出 `[8,9,0,1]`

递归过程：

```
add(9,9,0) → sum=18 → node(8, add(9,9,1))
add(9,9,1) → sum=19 → node(9, add(9,null,1))
add(9,null,1) → sum=10 → node(0, add(null,null,1))
add(null,null,1) → sum=1 → node(1, add(null,null,0))
add(null,null,0) → return null
```

最终链表：`8 → 9 → 0 → 1`

---

### ⏱️ 复杂度分析：

- **时间复杂度**：O(max(m, n))  
    —— m、n 为两链表长度，每个节点访问一次
- **空间复杂度**：O(max(m, n))  
    —— 递归调用栈深度等于结果链表长度（最坏情况）

> 💡 若用迭代法可将空间优化至 O(1)，但递归更简洁、易理解。

---

## 📚 笔记总结

### 🧩 问题特点：

- 数字以**逆序链表**存储（个位在头）；
- 需**模拟加法**，处理**进位**；
- 链表长度可能不同，结果可能更长。

### 💡 核心思想：

> **递归模拟竖式加法 + 进位传递**

### ✅ 算法步骤：

1. 递归函数接收 `l1`, `l2`, `carry`；
2. 终止条件：`l1==null && l2==null && carry==0`；
3. 计算当前位总和：`sum = carry + (l1?.val) + (l2?.val)`；
4. 创建新节点：`val = sum % 10`；
5. 递归处理下一位：`next = addTwo(l1.next, l2.next, sum / 10)`。

### 🔑 关键技巧：

- **递归天然处理链表结构**；
- **进位作为参数传递**，避免全局变量；
- **自动处理不同长度与末尾进位**。

### ⏱️ 复杂度：

- 时间：O(max(m, n))
- 空间：O(max(m, n))（递归栈）

### 💡 延伸思考：

- 迭代写法？→ 用 while 循环 + dummy 节点；
- 正序存储？→ 需用栈或反转链表（LeetCode 445）。

---

# 三、代码演示

## Java代码

```Java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        // 从头节点开始相加，初始进位为 0
        return addTwo(l1, l2, 0);
    }

    /**
     * 递归函数：合并两个链表当前位置的值，并处理进位
     * @param l1 当前 l1 链表节点（可能为 null）
     * @param l2 当前 l2 链表节点（可能为 null）
     * @param carry 来自低位的进位（0 或 1）
     * @return 新链表的当前节点
     */
    private ListNode addTwo(ListNode l1, ListNode l2, int carry) {
        // 🧩 递归终止条件：
        // 当两个链表都遍历完，且没有进位时，不再创建新节点
        if (l1 == null && l2 == null && carry == 0) {
            return null;
        }

        // 当前位的总和 = 进位 + l1.val（如果有） + l2.val（如果有）
        int sum = carry;

        // 如果 l1 不为空，加上它的值，并移动指针
        if (l1 != null) {
            sum += l1.val;
            l1 = l1.next;
        }

        // 如果 l2 不为空，加上它的值，并移动指针
        if (l2 != null) {
            sum += l2.val;
            l2 = l2.next;
        }

        // 📌 创建当前节点：
        // - 节点值 = sum % 10（个位）
        // - 下一个节点 = 递归处理剩余部分，进位 = sum / 10（十位）
        return new ListNode(sum % 10, addTwo(l1, l2, sum / 10));
    }
}
```

![2_两数相加_Java]()

## Python代码

```Python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
        return self.addTwo(l1, l2, 0)
    
    def addTwo(self, l1: Optional[ListNode], l2: Optional[ListNode], carry: int) -> Optional[ListNode]:
        # 递归边界：两个链表都为空且无进位
        if l1 is None and l2 is None and carry == 0:
            return None
        
        s = carry
        if l1 is not None:
            s += l1.val
            l1 = l1.next
        if l2 is not None:
            s += l2.val
            l2 = l2.next
        
        # 创建当前节点，递归处理下一位
        return ListNode(s % 10, self.addTwo(l1, l2, s // 10))
```



## C++代码

```C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
        return addTwo(l1, l2, 0);
    }

private:
    ListNode* addTwo(ListNode* l1, ListNode* l2, int carry) {
        // 递归边界：两个链表都为空且无进位
        if (l1 == nullptr && l2 == nullptr && carry == 0) {
            return nullptr;
        }

        int s = carry;
        if (l1 != nullptr) {
            s += l1->val;
            l1 = l1->next;
        }
        if (l2 != nullptr) {
            s += l2->val;
            l2 = l2->next;
        }

        // 创建当前节点，递归处理下一位
        return new ListNode(s % 10, addTwo(l1, l2, s / 10));
    }
};
```

