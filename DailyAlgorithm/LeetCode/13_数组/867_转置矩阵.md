本文题解基于以下原题整理，题意经本人重新表述，解题思路与代码均为原创或转载，仅用于个人学习与交流，完整题目请务必查阅官方原题链接：[867. 转置矩阵 - 力扣（LeetCode）](https://leetcode.cn/problems/transpose-matrix/description/?envType=study-plan-v2&envId=primers-list)

>标签：数组  |  矩阵  |  模拟

# 一、题目描述

## **问题概括**

给定一个 `m` 行 `n` 列的二维整数矩阵 `matrix`，你需要返回该矩阵的**转置矩阵**。转置操作是指将矩阵的行与列互换：原矩阵中位于第 `i` 行、第 `j` 列的元素，在转置矩阵中应位于第 `j` 行、第 `i` 列。

![img](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/13_%E6%95%B0%E7%BB%84/Images/867_%E8%BD%AC%E7%BD%AE%E7%9F%A9%E9%98%B5.png)

## **示例说明**

- **示例1**：输入 `matrix = [[1,2,3],[4,5,6],[7,8,9]]`，输出 `[[1,4,7],[2,5,8],[3,6,9]]`。
- **示例2**：输入 `matrix = [[1,2,3],[4,5,6]]`，输出 `[[1,4],[2,5],[3,6]]`。

## **数据范围**

- 矩阵的行数 `m` 和列数 `n` 满足 `1 <= m, n <= 1000`。
- 矩阵中元素总数满足 `1 <= m * n <= 10^5`。
- 矩阵中的每个元素值在 `-10^9` 到 `10^9` 之间。



# 二、解题思路\解题过程

这也是一道简单的编程题，简答题简单做，题目给的是`m`行`n`列的矩阵，那么我们就可以创建一个`n`行`m`列的空矩阵，只需要将两个数组的行列换一下就可以了，代码如下：
```cpp
class Solution {
public:
    vector<vector<int>> transpose(vector<vector<int>>& matrix) {
        // m是行数，n是列数
        int m = matrix.size(), n = matrix[0].size();
        vector<vector<int>> arr(n, vector<int>(m));
        for(int i = 0; i < m; i++){
            for(int j = 0; j < n; j++){
                arr[j][i] = matrix[i][j];
            }
        }
        return arr;
    }
};
```

```python
class Solution:
    def transpose(self, matrix: List[List[int]]) -> List[List[int]]:
        # m是行数，n是列数
        m, n = len(matrix), len(matrix[0])
        arr = [[0] * m for _ in range(n)]
        for i in range(m):
            for j in range(n):
                arr[j][i] = matrix[i][j]
        return arr
```
【注】：
**1、详解：`arr = [[0] * m for _ in range(n)]`**

这行代码的目的是：**创建一个 n 行 m 列的二维列表，所有元素初始化为 0**。

我们拆解来看：

🔹 1. `[0] * m`
- 创建一个包含 `m` 个 `0` 的**一维列表**
- 例如：`m = 2` → `[0, 0]`

🔹 2. `for _ in range(n)`
- 循环 `n` 次（`_` 表示“**不关心循环变量的值**”，<u>只是重复 n 次</u>）
- 每次都生成一个新的 `[0] * m` 列表

🔹 3. 整体：`[[0] * m for _ in range(n)]`
- 这是一个 **列表推导式（list comprehension）**
- 结果是一个包含 `n` 个子列表的列表
- 每个子列表长度为 `m`，内容为 `0`



# 三、代码演示

## Java代码

```Java
class Solution {
    public int[][] transpose(int[][] matrix) {
        // m是行数，n是列数
        int m = matrix.length, n = matrix[0].length;
        int[][] arr = new int[n][m];
        for(int i = 0; i < m; i++){
            for(int j = 0; j < n; j++){
                arr[j][i] = matrix[i][j];
            }
        }
        return arr;
    }
}
```



## Python代码

```Python
class Solution:
    def transpose(self, matrix: List[List[int]]) -> List[List[int]]:
        # m是行数，n是列数
        m, n = len(matrix), len(matrix[0])
        arr = [[0] * m for _ in range(n)]
        for i in range(m):
            for j in range(n):
                arr[j][i] = matrix[i][j]
        return arr
```



## C++代码

```C++
class Solution {
public:
    vector<vector<int>> transpose(vector<vector<int>>& matrix) {
        // m是行数，n是列数
        int m = matrix.size(), n = matrix[0].size();
        vector<vector<int>> arr(n, vector<int>(m));
        for(int i = 0; i < m; i++){
            for(int j = 0; j < n; j++){
                arr[j][i] = matrix[i][j];
            }
        }
        return arr;
    }
};
```

![867_转置矩阵_C++](https://gitee.com/lei-weijun/CodingLearning/raw/main/DailyAlgorithm/LeetCode/13_%E6%95%B0%E7%BB%84/Images/867_%E8%BD%AC%E7%BD%AE%E7%9F%A9%E9%98%B5_C++.png)